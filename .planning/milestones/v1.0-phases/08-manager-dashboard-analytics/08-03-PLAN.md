---
phase: 08-manager-dashboard-analytics
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/(dashboard)/analytics/individual/[id]/page.tsx
  - src/app/api/analytics/individual/[id]/route.ts
  - src/components/analytics/score-trend-chart.tsx
  - src/components/analytics/category-breakdown.tsx
  - src/components/analytics/session-comparison.tsx
  - src/components/analytics/period-selector.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements: [ANLT-01, ANLT-02, ANLT-03]

must_haves:
  truths:
    - "Line chart shows individual session scores over time for a specific report"
    - "Bar chart shows per-category average scores (wellbeing, engagement, performance, career)"
    - "Delta table shows session-over-session comparison with +/- score changes per category"
    - "Period selector offers fixed presets (30d, 3mo, 6mo, 1yr) plus custom date range"
    - "Reports (members) can see their own individual analytics"
    - "Analytics nav item appears in sidebar"
  artifacts:
    - path: "src/app/(dashboard)/analytics/page.tsx"
      provides: "Analytics overview page"
      min_lines: 30
    - path: "src/app/(dashboard)/analytics/individual/[id]/page.tsx"
      provides: "Individual analytics page with charts"
      min_lines: 50
    - path: "src/components/analytics/score-trend-chart.tsx"
      provides: "Recharts line chart for score trends"
      min_lines: 40
    - path: "src/components/analytics/category-breakdown.tsx"
      provides: "Recharts bar chart for category averages"
      min_lines: 40
    - path: "src/components/analytics/session-comparison.tsx"
      provides: "Delta table for session comparison"
      min_lines: 40
    - path: "src/components/analytics/period-selector.tsx"
      provides: "Period filter with presets and custom range"
      min_lines: 30
  key_links:
    - from: "src/app/(dashboard)/analytics/individual/[id]/page.tsx"
      to: "src/lib/analytics/queries.ts"
      via: "Server Component data fetch"
      pattern: "getScoreTrend|getCategoryAverages"
    - from: "src/components/analytics/score-trend-chart.tsx"
      to: "src/app/api/analytics/individual/[id]/route.ts"
      via: "TanStack Query on period change"
      pattern: "useQuery.*analytics"
---

<objective>
Individual analytics pages with score trend, category breakdown, and session comparison

Purpose: Managers and reports can see individual performance trends via charts. This is the core analytics experience -- line charts for score trends, bar charts for category breakdown, and a delta table for session-over-session comparison. Includes the analytics overview page and sidebar navigation.
Output: Analytics overview page, individual analytics page with 3 chart types, period selector, sidebar nav item
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manager-dashboard-analytics/08-01-PLAN.md
@src/lib/analytics/queries.ts
@src/lib/analytics/constants.ts
@src/components/session/score-sparkline.tsx
@src/components/layout/sidebar.tsx
@src/lib/db/schema/analytics.ts

<interfaces>
<!-- From Plan 08-01 query layer (will be created by Plan 01) -->
From src/lib/analytics/queries.ts (created in Plan 01):
- getScoreTrend(tx, userId, startDate, endDate) => Array<{ date: string; score: number }>
- getCategoryAverages(tx, userId, startDate, endDate) => Array<{ category: string; avgScore: number; sampleCount: number }>
- getSessionComparison(tx, sessionId1, sessionId2) => Array<{ category: string; score1: number; score2: number; delta: number }>

From src/lib/analytics/constants.ts (created in Plan 01):
- METRIC_NAMES: { SESSION_SCORE, WELLBEING, ENGAGEMENT, PERFORMANCE, CAREER, FEEDBACK, MOOD, ... }

From src/components/layout/sidebar.tsx:
- Sidebar with navItems array, role-based visibility
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Period selector, analytics overview page, and sidebar nav</name>
  <files>src/components/analytics/period-selector.tsx, src/app/(dashboard)/analytics/page.tsx, src/components/layout/sidebar.tsx</files>
  <action>
1. Create `src/components/analytics/period-selector.tsx` (Client Component):
   - Shared component used across all analytics pages
   - Fixed presets: "Last 30 days", "Last 3 months", "Last 6 months", "Last year" as Select dropdown
   - "Custom" option opens a date range picker (two date inputs: from/to)
   - Props: `value: PeriodValue`, `onChange: (period: PeriodValue) => void`
   - PeriodValue type: `{ preset: string; startDate: Date; endDate: Date }`
   - Helper function `periodToDateRange(preset: string)` computes start/end dates from preset strings
   - Export PeriodValue type for consumer use

2. Create `src/app/(dashboard)/analytics/page.tsx`:
   - Analytics overview Server Component
   - Auth check: managers see their direct reports list, admins see all users, members see only themselves
   - Show a list/grid of reports with links to `/analytics/individual/[userId]`
   - Each card: user name, avatar, latest session score (if exists), number of sessions completed
   - For members: auto-redirect to `/analytics/individual/${userId}` (they can only see themselves)
   - Empty state: "No session data available yet. Complete sessions to see analytics."

3. Add "Analytics" nav item to sidebar:
   - Add to navItems array with BarChart3 icon from lucide-react
   - Path: `/analytics`
   - Visible to all roles (members see own analytics per locked decision)
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Period selector component ready, analytics overview page lists reports with links, sidebar has Analytics nav item</done>
</task>

<task type="auto">
  <name>Task 2: Individual analytics page with score trend, category breakdown, and session comparison</name>
  <files>src/app/(dashboard)/analytics/individual/[id]/page.tsx, src/app/api/analytics/individual/[id]/route.ts, src/components/analytics/score-trend-chart.tsx, src/components/analytics/category-breakdown.tsx, src/components/analytics/session-comparison.tsx</files>
  <action>
1. Create `src/components/analytics/score-trend-chart.tsx` (Client Component):
   - Recharts LineChart wrapped in ResponsiveContainer
   - Parent div with explicit height: `h-[300px]` desktop, `h-[200px]` mobile via responsive classes
   - XAxis: date labels, YAxis: score 1-5 domain
   - Single line for individual score, color `hsl(var(--primary))`
   - Tooltip showing exact score and date
   - If fewer than 3 data points: show partial chart + subtle muted text "More data after 3+ sessions"
   - Props: `{ data: Array<{ date: string; score: number }>; loading?: boolean }`

2. Create `src/components/analytics/category-breakdown.tsx` (Client Component):
   - Recharts BarChart with horizontal bars (category name on Y, score on X)
   - Each category bar colored using HSL rotation from primary: rotate hue by 60deg steps
   - Domain: 1-5 on X axis
   - Show sample count as subtle text next to each bar
   - Insufficient data: categories with sampleCount < 3 shown with dashed bar + "Limited data" label
   - Props: `{ data: Array<{ category: string; avgScore: number; sampleCount: number }> }`

3. Create `src/components/analytics/session-comparison.tsx` (Client Component):
   - Delta table component (not chart -- per locked decision)
   - Two session selectors at top (dropdowns listing completed sessions for this report)
   - Table rows: Category | Session 1 Score | Session 2 Score | Delta
   - Delta column: green with up arrow for positive, red with down arrow for negative, gray dash for zero
   - Display delta as "+0.5" or "-0.3" format
   - Props: `{ sessions: Array<{ id: string; date: string; number: number }>; onCompare: (id1: string, id2: string) => void; comparisonData: Array<{ category: string; score1: number; score2: number; delta: number }> | null }`

4. Create API route `src/app/api/analytics/individual/[id]/route.ts`:
   - GET handler returning analytics data for a specific user
   - Query params: `period` (preset string), `startDate`, `endDate`, `compare` (sessionId1,sessionId2)
   - Auth check: user must be the person themselves, their manager, or an admin
   - Returns: `{ scoreTrend, categoryAverages, sessions (for comparison selector) }`
   - When `compare` param present: also returns `comparison` data

5. Create `src/app/(dashboard)/analytics/individual/[id]/page.tsx`:
   - Server Component that loads initial data for default period (last 3 months)
   - Auth check: viewer must be the user, their manager, or admin. Members can only view their own.
   - Renders: user name/title header, PeriodSelector, ScoreTrendChart, CategoryBreakdown, SessionComparison
   - Client wrapper component handles period changes via TanStack Query fetching from the API route
   - Pass initialData to charts for instant first render
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Individual analytics page shows score trend line chart, category bar chart, and session comparison delta table with period selector</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- Analytics overview page renders report list (or auto-redirects for members)
- Individual analytics page shows 3 chart/table types
- Period selector changes update chart data via API
- Sidebar shows Analytics nav item
- Members can only see their own analytics
</verification>

<success_criteria>
- Line chart displays session scores over time with proper axes and tooltips
- Bar chart displays per-category averages with color coding
- Delta table allows selecting two sessions and shows +/- score changes per category
- Period selector offers 30d/3mo/6mo/1yr presets plus custom range
- RBAC enforced: members see own data, managers see reports, admins see all
</success_criteria>

<output>
After completion, create `.planning/phases/08-manager-dashboard-analytics/08-03-SUMMARY.md`
</output>
