---
phase: 08-manager-dashboard-analytics
plan: 05
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/components/analytics/velocity-chart.tsx
  - src/components/analytics/adherence-chart.tsx
  - src/components/analytics/csv-export-button.tsx
  - src/app/api/analytics/export/route.ts
  - src/lib/analytics/csv.ts
  - src/lib/analytics/queries.ts
autonomous: true
requirements: [ANLT-06, ANLT-07, ANLT-09]

must_haves:
  truths:
    - "Action item velocity chart shows average time from creation to completion over time"
    - "Meeting adherence chart shows percentage of scheduled sessions completed per month"
    - "Full CSV export downloads all session data for selected period"
    - "Per-view CSV export downloads data currently displayed in a chart/table"
  artifacts:
    - path: "src/components/analytics/velocity-chart.tsx"
      provides: "Action item velocity area chart"
      min_lines: 40
    - path: "src/components/analytics/adherence-chart.tsx"
      provides: "Meeting adherence stacked bar chart"
      min_lines: 40
    - path: "src/app/api/analytics/export/route.ts"
      provides: "CSV export endpoint"
      exports: ["GET"]
    - path: "src/lib/analytics/csv.ts"
      provides: "CSV generation utilities"
      exports: ["generateCSV", "escapeCsvField"]
  key_links:
    - from: "src/app/api/analytics/export/route.ts"
      to: "src/lib/analytics/csv.ts"
      via: "CSV generation"
      pattern: "generateCSV"
    - from: "src/components/analytics/csv-export-button.tsx"
      to: "src/app/api/analytics/export/route.ts"
      via: "fetch download"
      pattern: "api/analytics/export"
---

<objective>
Action item velocity, meeting adherence charts, and CSV export

Purpose: Complete the analytics feature set with operational charts (velocity and adherence) and data export capability. These charts help managers understand team efficiency and meeting discipline beyond individual session scores.
Output: Two chart components, CSV export endpoint, export button component, CSV utilities
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manager-dashboard-analytics/08-01-PLAN.md
@.planning/phases/08-manager-dashboard-analytics/08-03-PLAN.md
@src/lib/analytics/queries.ts
@src/lib/analytics/constants.ts
@src/components/analytics/period-selector.tsx
@src/lib/db/schema/action-items.ts
@src/lib/db/schema/sessions.ts

<interfaces>
From src/components/analytics/period-selector.tsx:
- PeriodSelector component with PeriodValue type
- periodToDateRange(preset: string) utility

From src/lib/db/schema/action-items.ts:
- actionItems table: id, tenantId, sessionId, title, assigneeId, dueDate, status, createdAt, completedAt, ...

From src/lib/db/schema/sessions.ts:
- sessions table: id, seriesId, tenantId, scheduledAt, completedAt, status, sessionScore, ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Velocity and adherence queries, chart components</name>
  <files>src/lib/analytics/queries.ts, src/components/analytics/velocity-chart.tsx, src/components/analytics/adherence-chart.tsx</files>
  <action>
1. Add to `src/lib/analytics/queries.ts`:

   - `getActionItemVelocity(tx, userId, role, startDate, endDate)`:
     - Query action items with status='completed' and completedAt within range
     - Group by month: AVG of (completedAt - createdAt) in days
     - For managers: filter by items in their series. For admins: org-wide
     - Return: `Array<{ month: string; avgDays: number; count: number }>`

   - `getMeetingAdherence(tx, userId, role, startDate, endDate)`:
     - Query sessions grouped by month
     - Count per status: completed, cancelled, missed (status not 'scheduled' or 'in_progress')
     - Compute adherence: completed / total * 100
     - For managers: their series. For admins: org-wide
     - Return: `Array<{ month: string; completed: number; cancelled: number; missed: number; total: number; adherencePercent: number }>`

2. Create `src/components/analytics/velocity-chart.tsx` (Client Component):
   - Recharts AreaChart (per research recommendation: area chart emphasizes speed/slowness trend)
   - XAxis: months, YAxis: average days to completion
   - Filled area with gradient from primary color (opacity 0.3 at bottom, 0.8 at top)
   - Tooltip: "Month: {month}, Avg: {avgDays} days, Items: {count}"
   - Wrapper div with `h-[300px]` height
   - Lower is better -- optionally add a subtle reference line at a "good" threshold (7 days)
   - Props: `{ data: Array<{ month: string; avgDays: number; count: number }> }`

3. Create `src/components/analytics/adherence-chart.tsx` (Client Component):
   - Recharts stacked BarChart (per research recommendation and docs/analytics.md)
   - XAxis: months, YAxis: count of sessions
   - Three stacked segments: completed (green), cancelled (amber), missed (red)
   - Colors: completed = hsl(142,76%,36%), cancelled = hsl(38,92%,50%), missed = hsl(0,72%,51%)
   - Legend showing all three statuses
   - Secondary YAxis or annotation showing adherence percentage
   - Tooltip: "Month: {month}, Completed: N, Cancelled: N, Missed: N, Adherence: N%"
   - Props: `{ data: Array<{ month: string; completed: number; cancelled: number; missed: number; adherencePercent: number }> }`
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Velocity area chart and adherence stacked bar chart render with proper data, query functions added</done>
</task>

<task type="auto">
  <name>Task 2: CSV export endpoint, utility, and export buttons on analytics pages</name>
  <files>src/lib/analytics/csv.ts, src/app/api/analytics/export/route.ts, src/components/analytics/csv-export-button.tsx</files>
  <action>
1. Create `src/lib/analytics/csv.ts`:
   - `escapeCsvField(value: unknown): string` -- handle null, commas, quotes, newlines
   - `generateCSV(headers: string[], rows: unknown[][]): string` -- join with proper escaping
   - `sessionDataToRows(sessions)` -- transforms session data into CSV rows with columns: Date, Report, Session #, Score, Categories (one column per), Notes summary, Action items count

2. Create `src/app/api/analytics/export/route.ts`:
   - GET handler with auth check
   - Query params:
     - `type`: "full" | "score-trend" | "categories" | "velocity" | "adherence"
     - `userId` (optional, for individual exports)
     - `teamId` (optional, for team exports)
     - `startDate`, `endDate` (period)
   - For "full": fetch all session data with answers, notes, action items for the period
     - Columns: Date, Report, Manager, Template, Score, Duration, Status, Action Items Created, Action Items Completed, AI Summary (first 200 chars)
   - For per-view types: fetch only the relevant data and format as CSV
   - Return Response with headers:
     - `Content-Type: text/csv; charset=utf-8`
     - `Content-Disposition: attachment; filename="1on1-analytics-{type}-{date}.csv"`
   - RBAC: managers export only their reports' data, admins export org-wide, members export only their own

3. Create `src/components/analytics/csv-export-button.tsx` (Client Component):
   - Button with Download icon from lucide-react
   - Props: `{ type: string; userId?: string; teamId?: string; period: PeriodValue; label?: string }`
   - On click: fetch from `/api/analytics/export?type=...&...` and trigger browser download
   - Show loading spinner during download, success toast via sonner on complete
   - Two variants: subtle icon button for per-view (placed on each chart card), and full button for "Export All"

4. Add CsvExportButton to the individual analytics page (Plan 03's page):
   - Per-view export button on ScoreTrendChart card, CategoryBreakdown card
   - "Export All Data" button in page header
   - Also add velocity and adherence charts + export buttons to the individual analytics page (these charts apply to individual users too, not just team level)
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>CSV export endpoint supports full and per-view exports, export buttons placed on analytics pages, velocity and adherence charts integrated into individual analytics</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- Velocity area chart renders with monthly average completion days
- Adherence stacked bar chart renders with completed/cancelled/missed segments
- CSV export endpoint returns valid CSV with proper Content-Type and Content-Disposition
- Per-view and full export both work
- RBAC enforced on export endpoint
</verification>

<success_criteria>
- Action item velocity chart shows trend of average days to completion
- Meeting adherence chart shows completed vs missed/cancelled session ratio per month
- Full CSV export downloads comprehensive session data
- Per-view CSV export downloads data for the specific chart being viewed
- Export triggers browser download with toast notification
</success_criteria>

<output>
After completion, create `.planning/phases/08-manager-dashboard-analytics/08-05-SUMMARY.md`
</output>
