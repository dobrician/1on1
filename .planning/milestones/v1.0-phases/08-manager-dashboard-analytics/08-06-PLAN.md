---
phase: 08-manager-dashboard-analytics
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/analytics/page.tsx
  - src/lib/analytics/constants.ts
  - src/lib/analytics/compute.ts
  - src/lib/analytics/queries.ts
  - src/app/api/analytics/export/route.ts
autonomous: true
gap_closure: true
requirements: [ANLT-01, ANLT-02, ANLT-03, ANLT-09]

must_haves:
  truths:
    - "Analytics overview page loads without server error"
    - "Score trend chart shows connecting line between data points"
    - "Category breakdown shows data from actual template sections, not hardcoded English names"
    - "Session comparison shows category scores from two selected sessions"
    - "CSV export downloads with correct UTF-8 encoding for non-ASCII characters"
  artifacts:
    - path: "src/app/(dashboard)/analytics/page.tsx"
      provides: "Fixed analytics overview query (no ungrouped column error)"
    - path: "src/lib/analytics/constants.ts"
      provides: "CATEGORY_METRICS removed as runtime filter"
    - path: "src/lib/analytics/compute.ts"
      provides: "Dynamic category snapshot computation from template sections"
    - path: "src/lib/analytics/queries.ts"
      provides: "Fixed parseFloat, dynamic category queries"
    - path: "src/app/api/analytics/export/route.ts"
      provides: "UTF-8 BOM prefix on CSV response"
  key_links:
    - from: "src/lib/analytics/compute.ts"
      to: "src/lib/analytics/constants.ts"
      via: "No longer filtering by CATEGORY_METRICS keys"
      pattern: "SCORABLE_ANSWER_TYPES"
    - from: "src/lib/analytics/queries.ts"
      to: "template_sections"
      via: "Dynamic category names from DB"
      pattern: "templateSections.name"
---

<objective>
Fix core analytics query and data layer bugs found during UAT: analytics page crash (ungrouped column), score trend NaN, hardcoded category names, and CSV encoding.

Purpose: These are foundational query-layer fixes that team analytics (plan 07) depends on.
Output: Working analytics overview page, correct charts, proper CSV export.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/analytics/constants.ts
@src/lib/analytics/compute.ts
@src/lib/analytics/queries.ts
@src/app/(dashboard)/analytics/page.tsx
@src/app/api/analytics/export/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix analytics page crash, score trend NaN, and CSV encoding</name>
  <files>src/app/(dashboard)/analytics/page.tsx, src/lib/analytics/queries.ts, src/app/api/analytics/export/route.ts</files>
  <action>
    **Fix 1 - Analytics page ungrouped column (blocker, UAT test 5):**
    In `src/app/(dashboard)/analytics/page.tsx` lines 61-69, the latestScore correlated subquery references `meetingSeries.id` inside a GROUP BY that includes `meetingSeries.id` -- but PostgreSQL rejects `s2.session_score` as ungrouped in that subquery context. Fix by computing latestScore in app code after the main query:
    - Remove the latestScore subquery from the SQL select
    - After getting reportRows, do a second query (or use a lateral join) to get the latest score per series
    - Simplest fix: query latest scores separately with a single query joining sessions + meeting_series, grouped by report_id, using `DISTINCT ON (report_id) ORDER BY completed_at DESC`
    - Then merge latestScore into the report map in the JS deduplication loop

    **Fix 2 - Score trend parseFloat NaN (UAT test 7):**
    In `src/lib/analytics/queries.ts`:
    - Lines 87-91 (snapshot path) and 114-119 (live path): Replace `parseFloat(r.score!)` with `Number(r.score)`
    - Add `.filter(r => !isNaN(r.score))` after the `.map()` calls in both getScoreTrend paths
    - Apply same Number() fix to all other parseFloat calls on Drizzle decimal strings in the file (getCategoryAverages, getSessionComparison, getTeamAverages, getTeamHeatmapData, getActionItemVelocity)

    **Fix 3 - CSV encoding (UAT test 16):**
    In `src/app/api/analytics/export/route.ts` around line 271, prepend UTF-8 BOM to the response body:
    - Change `new Response(result.csv, ...)` to `new Response('\uFEFF' + result.csv, ...)`
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck</automated>
  </verify>
  <done>Analytics page loads without PostgreSQL error. Score trend chart data has no NaN values. CSV export includes UTF-8 BOM for correct encoding in Excel.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hardcoded category filter -- derive categories dynamically from template sections</name>
  <files>src/lib/analytics/constants.ts, src/lib/analytics/compute.ts, src/lib/analytics/queries.ts</files>
  <action>
    **Root cause:** CATEGORY_METRICS in constants.ts only has 6 English keys (wellbeing, engagement, performance, career, feedback, mood). Actual templates have 13 section names including Romanian. 10/13 sections are silently dropped.

    **Fix constants.ts:**
    - Keep METRIC_NAMES for operational metrics only (session_score, action_completion_rate, meeting_adherence)
    - Remove CATEGORY_METRICS entirely (or keep only as a display-name helper, NOT as a runtime filter)
    - Add a helper function `categoryToMetricName(sectionName: string): string` that converts any section name to a metric name by slugifying: `sectionName.toLowerCase().trim().replace(/\s+/g, '_') + '_score'`
    - Keep SCORABLE_ANSWER_TYPES as-is

    **Fix compute.ts (lines 106-108):**
    - Remove the `if (!metricName) continue;` guard that skips sections not in CATEGORY_METRICS
    - Instead, use the new `categoryToMetricName()` helper to generate a metric name for ANY template section
    - Compute and store snapshot rows for every section that has scorable answers, regardless of name

    **Fix queries.ts:**
    - `getCategoryAverages()`: Remove `inArray(analyticsSnapshots.metricName, categoryMetricNames)` filter from snapshot path. Instead, filter snapshot rows where metricName ends with `_score` and is NOT `session_score` (operational metric)
    - In the live fallback path, the query already groups by `templateSections.name` which is correct -- just ensure no CATEGORY_METRICS filtering is applied
    - `getSessionComparison()`: Already queries live by templateSections.name -- verify no CATEGORY_METRICS filter is applied (it should be fine after the constants change)
    - `getTeamAverages()`: Same fix as getCategoryAverages snapshot path -- remove hardcoded metric name filter
    - `getTeamHeatmapData()`: Same fix -- remove hardcoded metric name filter

    **Important:** The category display name in query results should be the original `templateSections.name` (properly cased), not the slugified metric name. When reading from snapshots, reverse the slug: `metricName.replace(/_score$/, '').replace(/_/g, ' ')` and capitalize first letters, OR store the original section name in a new approach.

    Simpler alternative for snapshots: store the section name directly as the metricName (e.g., "Wellbeing" instead of "wellbeing_score"). This avoids lossy slug-to-display conversion. Adjust compute.ts to use section name as metricName, and queries to filter out operational metrics (session_score, action_completion_rate, meeting_adherence) by excluding them from category results.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run build</automated>
  </verify>
  <done>Category breakdown shows all template sections with scorable answers. Session comparison works with any section name. No data is silently dropped due to name mismatch.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run build` succeeds
- Analytics page loads without crash (no ungrouped column error)
- Category queries return data for all template sections, not just 6 hardcoded ones
</verification>

<success_criteria>
UAT tests 5, 7, 8, 9, 16 are resolved. Analytics overview loads. Score trend shows lines. Categories are dynamic. CSV has correct encoding.
</success_criteria>

<output>
After completion, create `.planning/phases/08-manager-dashboard-analytics/08-06-SUMMARY.md`
</output>
