---
phase: 08-manager-dashboard-analytics
plan: 07
type: execute
wave: 2
depends_on: ["08-06"]
files_modified:
  - src/lib/analytics/queries.ts
  - src/lib/db/seed.ts
autonomous: true
gap_closure: true
requirements: [ANLT-04, ANLT-05, ANLT-06, ANLT-07, ANLT-08]

must_haves:
  truths:
    - "Team analytics page shows aggregated category scores from live session data"
    - "Team heatmap shows SVG dot matrix with per-member per-category scores"
    - "Anonymization toggle replaces member names when enabled"
    - "Velocity chart shows action item completion data for seeded reports"
    - "MemberCount>=3 filter only applies when anonymize=true"
  artifacts:
    - path: "src/lib/analytics/queries.ts"
      provides: "Live fallback for getTeamAverages and getTeamHeatmapData; fixed manager roleFilter"
    - path: "src/lib/db/seed.ts"
      provides: "Completed action items for Dave; analytics snapshot seed data"
  key_links:
    - from: "src/lib/analytics/queries.ts"
      to: "session_answers + template_sections"
      via: "Live fallback queries joining through sessions"
      pattern: "sessionAnswers.*templateSections"
    - from: "src/lib/db/seed.ts"
      to: "analytics_snapshots"
      via: "seedAnalyticsSnapshots function"
      pattern: "analyticsSnapshots"
---

<objective>
Fix team analytics (no data, no heatmap, broken anonymization) and velocity chart (empty) by adding live query fallbacks and fixing seed data.

Purpose: Team analytics and velocity chart have zero data because they only query analytics_snapshots (always empty in dev) with no live fallback.
Output: Working team analytics page with heatmap, working velocity chart, proper seed data.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manager-dashboard-analytics/08-06-SUMMARY.md
@src/lib/analytics/queries.ts
@src/lib/db/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live fallback to team queries and fix velocity roleFilter</name>
  <files>src/lib/analytics/queries.ts</files>
  <action>
    **Fix 1 - getTeamAverages() live fallback (UAT tests 11, 13):**
    After the existing snapshot query (which returns [] when analytics_snapshots is empty), add a live fallback block identical to the pattern used in getCategoryAverages():
    - Get team member userIds (already done in existing code)
    - Query session_answers joined with template_questions, template_sections, sessions, meeting_series
    - Filter: sessions.status = 'completed', answerNumeric IS NOT NULL, skipped = false, answerType IN SCORABLE_ANSWER_TYPES, meeting_series.reportId IN memberIds, completedAt within date range
    - Group by templateSections.name
    - Return CategoryAverage[] with avgScore and memberCount (use COUNT(DISTINCT meeting_series.reportId))

    **Fix 2 - Make memberCount>=3 filter conditional (UAT test 13):**
    The `.filter(r => r.memberCount >= 3)` on line 334 should ONLY apply when anonymization is required. Change getTeamAverages signature to accept an `anonymize: boolean` parameter. Only apply the memberCount>=3 filter when `anonymize === true`. Update callers to pass the anonymize flag.

    **Fix 3 - getTeamHeatmapData() live fallback (UAT tests 12, 13):**
    After the existing snapshot query, add live fallback:
    - Query session_answers joined with template_questions, template_sections, sessions, meeting_series
    - Filter same as above, but also group by meeting_series.reportId (userId)
    - Return per-user per-category scores with sampleCount
    - Apply anonymization in the same way as existing code (nameMap)

    **Fix 4 - Velocity roleFilter (UAT test 14):**
    In getActionItemVelocity() lines 446-451, the manager roleFilter uses raw SQL with wrong table names (`session` instead of Drizzle's table reference). Replace with Drizzle constructs:
    ```typescript
    roleFilter = inArray(
      actionItems.sessionId,
      tx.select({ id: sessions.id })
        .from(sessions)
        .innerJoin(meetingSeries, eq(sessions.seriesId, meetingSeries.id))
        .where(eq(meetingSeries.managerId, userId))
    );
    ```
    Import `inArray` if not already imported.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck</automated>
  </verify>
  <done>Team analytics queries return data from live session_answers when analytics_snapshots is empty. Velocity query uses proper Drizzle constructs. MemberCount filter is conditional on anonymize flag.</done>
</task>

<task type="auto">
  <name>Task 2: Fix seed data -- completed action items for Dave and analytics snapshots</name>
  <files>src/lib/db/seed.ts</files>
  <action>
    **Fix 1 - Seed completed action items for Dave (UAT test 14):**
    In seedActionItems(), add 3 completed action items assigned to Dave (DAVE_ID) with completedAt dates spread across the past 3 months. Use deterministic UUIDs (consistent with project convention). Example:
    ```
    - "Refactor authentication module" completed 2 months ago (took 5 days)
    - "Write unit tests for API endpoints" completed 1 month ago (took 3 days)
    - "Update deployment documentation" completed 2 weeks ago (took 2 days)
    ```
    Each needs: id, sessionId (use existing SESSION_1_ID, SESSION_2_ID, SESSION_3_ID), tenantId (ACME_TENANT_ID), assigneeId (DAVE_ID), createdById (BOB_ID), title, status: 'completed', completedAt, dueDate, category.

    **Fix 2 - Seed analytics snapshots (UAT test 11):**
    Add a `seedAnalyticsSnapshots()` function that populates the analytics_snapshots table with sample data. Use `computeSessionSnapshot` from `@/lib/analytics/compute` if possible, or insert directly:
    - For each completed session in seed data, compute and insert snapshot rows
    - Cover at least SESSION_1_ID, SESSION_2_ID, SESSION_3_ID
    - If direct insertion is simpler (no tenant context in seed), insert rows directly using the session data:
      - Get the session's completedAt for period calculation
      - Insert session_score metric + per-category metrics based on seed answer data
    - Call seedAnalyticsSnapshots() from the main seed function, after seedSessionAnswers()

    **Important:** Use deterministic UUIDs for snapshot rows (project convention for idempotent seeds). Use onConflictDoUpdate pattern consistent with other seed functions.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run build</automated>
  </verify>
  <done>Seed produces completed action items for Dave visible in velocity chart. Analytics snapshots table is populated for team analytics to query. Re-running seed is idempotent.</done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run build` succeeds
- Team analytics page shows category data from live or snapshot queries
- Velocity chart has data points for Dave's completed action items
- Anonymization toggle works when team data is visible
</verification>

<success_criteria>
UAT tests 11, 12, 13, 14 are resolved. Team analytics shows aggregated scores and heatmap. Velocity chart shows completion data. Anonymization toggle works.
</success_criteria>

<output>
After completion, create `.planning/phases/08-manager-dashboard-analytics/08-07-SUMMARY.md`
</output>
