---
phase: 10-integration-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/theme-toggle.tsx
  - src/app/globals.css
  - src/lib/theme-presets.ts
  - src/components/theme-color-provider.tsx
  - src/lib/db/schema/tenant.ts
  - src/app/api/tenant/settings/route.ts
  - src/app/(dashboard)/settings/company/page.tsx
  - src/components/analytics/category-breakdown.tsx
  - src/components/analytics/adherence-chart.tsx
  - src/components/analytics/velocity-chart.tsx
  - src/components/analytics/team-heatmap.tsx
  - src/components/analytics/team-overview.tsx
  - src/components/analytics/score-sparkline.tsx
  - src/components/analytics/score-trend-chart.tsx
  - src/components/session/wizard-top-bar.tsx
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "Dark mode toggle switches all screens between light and dark correctly"
    - "No hardcoded colors remain in chart components -- all use CSS variables"
    - "Admin can select an org color theme from company settings"
    - "Charts display monochrome palette derived from the active org theme"
    - "Theme toggle is accessible in both dashboard header and wizard top bar"
  artifacts:
    - path: "src/components/theme-toggle.tsx"
      provides: "Two-state light/dark toggle (simplified from three-state)"
      contains: "resolvedTheme"
    - path: "src/lib/theme-presets.ts"
      provides: "Org color theme presets (neutral, zinc, slate, stone, blue, green, rose, orange)"
      exports: ["THEME_PRESETS", "ThemePreset"]
    - path: "src/components/theme-color-provider.tsx"
      provides: "Client component that applies data-color-theme attribute to HTML element"
      exports: ["ThemeColorProvider"]
    - path: "src/app/globals.css"
      provides: "CSS variable overrides for each color theme preset, dark mode compositions"
      contains: "data-color-theme"
  key_links:
    - from: "src/components/theme-color-provider.tsx"
      to: "src/app/globals.css"
      via: "data-color-theme attribute triggers CSS variable overrides"
      pattern: "data-color-theme"
    - from: "src/components/analytics/*.tsx"
      to: "src/app/globals.css"
      via: "CSS variable references replacing hardcoded colors"
      pattern: "var\\(--chart-"
---

<objective>
Dark mode audit and fixes, org color theme infrastructure, and monochrome chart palette refactor.

Purpose: INFR-05 requires dark mode via Tailwind dark: variants. The org theme system enables per-organization visual identity. Chart refactor eliminates hardcoded colors that break in dark mode and don't respond to theme changes.

Output: Working dark/light toggle on all screens, org color theme picker in company settings, all charts using CSS variable-based monochrome palette.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-polish/10-CONTEXT.md
@.planning/phases/10-integration-polish/10-RESEARCH.md
@src/app/globals.css
@src/components/theme-toggle.tsx
@src/components/theme-provider.tsx
@src/components/analytics/category-breakdown.tsx
@src/components/analytics/adherence-chart.tsx
@src/components/analytics/velocity-chart.tsx
@src/components/analytics/team-heatmap.tsx
@src/components/analytics/team-overview.tsx
@src/components/analytics/score-sparkline.tsx
@src/components/analytics/score-trend-chart.tsx
@src/components/session/wizard-top-bar.tsx
@src/lib/db/schema/tenant.ts
@src/app/api/tenant/settings/route.ts
@src/app/(dashboard)/settings/company/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark mode fixes, two-state toggle, and org color theme infrastructure</name>
  <files>
    src/components/theme-toggle.tsx,
    src/app/globals.css,
    src/lib/theme-presets.ts,
    src/components/theme-color-provider.tsx,
    src/lib/db/schema/tenant.ts,
    src/app/api/tenant/settings/route.ts,
    src/app/(dashboard)/settings/company/page.tsx,
    src/components/session/wizard-top-bar.tsx,
    src/app/layout.tsx
  </files>
  <action>
    **1. Simplify ThemeToggle to two-state (light/dark):**
    - Remove three-state dropdown. Use single button: click toggles between light and dark.
    - Use `resolvedTheme` from next-themes (system preference resolves to light or dark on first visit).
    - Show Moon icon in light mode, Sun icon in dark mode. See code example in RESEARCH.md.

    **2. Add ThemeToggle to wizard top bar:**
    - In `wizard-top-bar.tsx`, add `<ThemeToggle />` in the top bar actions area (next to existing controls).

    **3. Dark mode audit and fix:**
    - Grep codebase for hardcoded color values: `hsl(`, `rgb(`, `#[0-9a-fA-F]{3,8}`, `oklch(` in `.tsx` files (exclude globals.css, node_modules).
    - For each hardcoded color found in components, replace with appropriate CSS variable reference (`var(--primary)`, `var(--muted-foreground)`, etc.) or Tailwind dark: variant class.
    - Special attention to Tiptap editor: add `.dark .ProseMirror` CSS overrides in globals.css if Tiptap doesn't inherit dark mode properly (background, text color, placeholder color, selection highlight).
    - Email templates: do NOT add dark mode forcing -- leave them color-neutral per locked decision.

    **4. Create org color theme infrastructure:**
    - Create `src/lib/theme-presets.ts` with theme preset definitions. Export `THEME_PRESETS` array and `ThemePreset` type. Include presets: neutral (default), zinc, slate, stone, blue, green, rose, orange. Each preset has: id, name, and description.
    - Add `colorTheme` to tenant settings validation schema (Zod). Default: "neutral". Add to PATCH settings endpoint.
    - Create `src/components/theme-color-provider.tsx`: client component that reads org colorTheme from a prop (passed down from server component or context), sets `data-color-theme` attribute on `document.documentElement`. Wrap in dashboard layout.
    - In `globals.css`, add CSS variable overrides scoped to `[data-color-theme="X"]` for each preset. Each overrides `--primary`, `--primary-foreground`, and `--chart-1` through `--chart-5`. Use oklch format. For the default "neutral" preset, no overrides needed (base CSS vars are already neutral).
    - Also add `.dark [data-color-theme="X"]` overrides with adjusted lightness for dark mode readability.
    - In company settings page, add a color theme selector (radio group or card grid showing color swatches for each preset). Save via existing PATCH /api/tenant/settings endpoint.

    **5. Wire ThemeColorProvider into layout:**
    - In dashboard layout, read tenant settings server-side, pass `colorTheme` to `ThemeColorProvider` which wraps the page content.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run lint && bun run build</automated>
  </verify>
  <done>
    - ThemeToggle is two-state (light/dark), works in both dashboard header and wizard top bar
    - No hardcoded color values remain in component .tsx files (all use CSS vars or Tailwind classes)
    - Tiptap editor renders correctly in dark mode
    - Company settings page has color theme selector with 8 presets
    - data-color-theme attribute applied to HTML element, CSS variable overrides active
    - Build passes with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Chart palette refactor to monochrome CSS variables</name>
  <files>
    src/components/analytics/category-breakdown.tsx,
    src/components/analytics/adherence-chart.tsx,
    src/components/analytics/velocity-chart.tsx,
    src/components/analytics/team-heatmap.tsx,
    src/components/analytics/team-overview.tsx,
    src/components/analytics/score-sparkline.tsx,
    src/components/analytics/score-trend-chart.tsx
  </files>
  <action>
    Refactor all chart components to use CSS variable-based monochrome palette instead of hardcoded colors.

    **For each chart component:**
    1. Remove all hardcoded HSL/oklch color constants (COLORS arrays, inline style colors).
    2. Replace with `var(--chart-1)` through `var(--chart-5)` CSS variable references in Recharts `fill`, `stroke`, and `style` props.
    3. For semantic colors (adherence chart: completed/cancelled/missed), define semantic CSS variables in globals.css: `--color-success`, `--color-warning`, `--color-destructive` using muted variants of the theme that work in both light and dark modes. Use these instead of hardcoded green/amber/red.

    **Specific files:**
    - `category-breakdown.tsx`: Replace 5 hardcoded HSL colors with `var(--chart-1)` through `var(--chart-5)`.
    - `adherence-chart.tsx`: Replace 3 hardcoded HSL colors (green/amber/red) with `var(--color-success)`, `var(--color-warning)`, `var(--color-destructive)`.
    - `velocity-chart.tsx`: Replace hardcoded green HSL with `var(--chart-1)`.
    - `team-heatmap.tsx`: Replace hardcoded conditional HSL colors with opacity-based scale using `var(--chart-1)` with CSS opacity. Use a single hue with varying opacity for the heatmap gradient.
    - `team-overview.tsx`: Replace hardcoded conditional HSL colors with CSS variable references.
    - `score-sparkline.tsx`: Fix `hsl(var(--primary))` to just `var(--primary)` (oklch format, not hsl wrapper).
    - `score-trend-chart.tsx`: Verify it uses CSS variables correctly; fix if needed.

    **Verify dark mode:** Each chart should render with readable colors in both light and dark modes. The CSS variables defined in Task 1's globals.css overrides handle this automatically.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run build</automated>
  </verify>
  <done>
    - Zero hardcoded HSL/oklch/rgb color values in any analytics chart component
    - All charts use var(--chart-N) or semantic CSS variables
    - Charts render correctly in both light and dark modes (colors are readable)
    - Charts respond to org color theme changes (switching theme changes chart palette)
  </done>
</task>

</tasks>

<verification>
- `bun run typecheck` passes
- `bun run build` succeeds
- Grep for hardcoded colors in chart components returns zero results: `grep -rn "hsl(" src/components/analytics/ | grep -v "var("` should produce no output
- Theme toggle switches between light and dark in both dashboard and wizard
</verification>

<success_criteria>
- INFR-05 (dark mode) is functional across all screens
- Org color theme infrastructure is in place with 8 presets
- All chart components use CSS variable-based monochrome palette
- Build and typecheck pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-polish/10-01-SUMMARY.md`
</output>
