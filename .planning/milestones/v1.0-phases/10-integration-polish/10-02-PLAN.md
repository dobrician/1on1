---
phase: 10-integration-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/top-nav.tsx
  - src/components/layout/sidebar.tsx
  - src/app/(dashboard)/layout.tsx
  - src/components/layout/user-menu.tsx
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "Main navigation is a horizontal top bar matching shadcn dashboard reference style"
    - "Primary nav items are visible in top bar, settings items are in a dropdown"
    - "Dashboard layout uses top nav with full-width content area below"
    - "Navigation works correctly on mobile with responsive collapse"
  artifacts:
    - path: "src/components/layout/top-nav.tsx"
      provides: "Horizontal top navigation bar with logo, nav links, settings dropdown, search, theme toggle, user menu"
      exports: ["TopNav"]
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Restructured dashboard layout with TopNav replacing Sidebar"
      contains: "TopNav"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/top-nav.tsx"
      via: "import and render TopNav"
      pattern: "import.*TopNav"
    - from: "src/components/layout/top-nav.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "ThemeToggle in header actions"
      pattern: "ThemeToggle"
---

<objective>
Restructure main dashboard navigation from left sidebar to horizontal top nav bar following the shadcn dashboard reference design.

Purpose: Match the clean, professional look of https://ui.shadcn.com/examples/dashboard with top navigation, giving content area full horizontal width.

Output: Top nav bar with logo, primary nav links, settings dropdown, search trigger, theme toggle, and user menu. Dashboard layout restructured to use TopNav instead of Sidebar.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-polish/10-CONTEXT.md
@.planning/phases/10-integration-polish/10-RESEARCH.md
@src/components/layout/sidebar.tsx
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create top navigation bar component</name>
  <files>
    src/components/layout/top-nav.tsx
  </files>
  <action>
    Create `src/components/layout/top-nav.tsx` as a client component ("use client") matching the shadcn dashboard reference style.

    **Structure:**
    ```
    <header> sticky top-0, z-50, h-14, border-b, bg-background
      Logo/brand link (left) -> links to /overview
      Primary nav links (center-left): Overview, Sessions, Action Items, History, Analytics
      Settings dropdown (after primary nav): DropdownMenu with People, Templates, Company, Audit Log
      Right actions (ml-auto): SearchTrigger, ThemeToggle, UserMenu
    </header>
    ```

    **Nav item behavior:**
    - Use `usePathname()` to determine active state.
    - Active: `bg-accent text-accent-foreground` on the link.
    - Inactive: `text-muted-foreground hover:text-foreground`.
    - Settings dropdown trigger shows "Settings" label. Highlight trigger if any settings sub-page is active.

    **RBAC visibility:**
    - Read role from session (use existing useSession or auth pattern from sidebar).
    - People, Templates, Audit Log: visible to admin and manager roles.
    - Company: visible to admin only.
    - Analytics: visible to admin and manager only.

    **Mobile responsive:**
    - On screens < md (768px): collapse nav links into a hamburger Sheet (slide-in from left) using shadcn Sheet component.
    - Keep SearchTrigger + ThemeToggle + UserMenu always visible in header.

    **Import existing components:** SearchTrigger (from command palette), ThemeToggle, and extract UserMenu from sidebar if inline, or create a standalone UserMenu dropdown showing user name/email, role badge, and sign out button.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck</automated>
  </verify>
  <done>
    - TopNav component renders with logo, primary nav, settings dropdown, and right actions
    - Active link highlighting works based on current pathname
    - RBAC controls visibility of admin/manager-only nav items
    - Mobile hamburger menu works on small screens
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure dashboard layout to use top nav</name>
  <files>
    src/app/(dashboard)/layout.tsx,
    src/components/layout/sidebar.tsx
  </files>
  <action>
    **1. Update dashboard layout:**
    - Remove `Sidebar` import and rendering from `(dashboard)/layout.tsx`.
    - Add `TopNav` component at the top of the layout.
    - Layout structure becomes: `<div className="min-h-screen flex flex-col"><TopNav /><main className="flex-1 p-6">{children}</main></div>`
    - Keep existing session validation and ThemeColorProvider wrapping (from plan 01 if already present, or prepare for it).
    - The main content area should use `max-w-7xl mx-auto` or similar container for readability on wide screens (matching shadcn dashboard reference).

    **2. Preserve sidebar as fallback (do not delete):**
    - Keep `sidebar.tsx` file but it is no longer imported by the dashboard layout.
    - Add a comment at the top: `// Legacy sidebar -- kept as icon-only collapsible fallback. Not used in default layout.`
    - This preserves the option to re-enable it later.

    **3. If UserMenu was embedded in sidebar:**
    - Extract it to `src/components/layout/user-menu.tsx` if not already standalone.
    - Import in TopNav. Show user avatar/initials, name, role, and sign out action.

    **4. Verify all navigation paths:**
    - Ensure all sidebar nav links are represented in TopNav (Overview, Sessions, Action Items, History, Analytics, People, Templates, Company, Audit Log).
    - Ensure sign-out action works from the new UserMenu location.

    **5. Build and verify:**
    - Run `bun run build` to ensure no broken imports or missing components.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run lint && bun run build</automated>
  </verify>
  <done>
    - Dashboard layout uses TopNav instead of Sidebar
    - All navigation paths preserved and functional
    - Content area has full horizontal width
    - Sign out works from UserMenu in top nav
    - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
- `bun run build` succeeds with no errors
- All dashboard routes accessible via top nav links
- Settings dropdown contains admin-only items
- Mobile responsive: hamburger menu appears on small screens
</verification>

<success_criteria>
- Dashboard matches shadcn dashboard reference layout pattern (top nav, full-width content)
- All existing navigation paths are preserved
- RBAC visibility rules enforced in nav
- Responsive on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-polish/10-02-SUMMARY.md`
</output>
