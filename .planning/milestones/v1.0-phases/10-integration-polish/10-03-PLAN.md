---
phase: 10-integration-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/components/session/wizard-shell.tsx
  - src/components/session/wizard-navigation.tsx
  - src/components/session/wizard-step-sidebar.tsx
  - src/components/session/context-panel.tsx
  - src/components/session/floating-context-widgets.tsx
  - src/components/session/wizard-top-bar.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "Wizard has a left vertical step sidebar showing all categories with completion indicators"
    - "Context panel is replaced by floating collapsible card widgets on the right"
    - "Prev/Next buttons are positioned near form content, not far away at viewport edges"
    - "On mobile, step sidebar collapses and context widgets use bottom sheet pattern"
    - "Slide transitions animate between category steps"
    - "Scrollbar-over-navigation bug is fixed"
  artifacts:
    - path: "src/components/session/wizard-step-sidebar.tsx"
      provides: "Left vertical sidebar showing category steps with completion status indicators"
      exports: ["WizardStepSidebar"]
    - path: "src/components/session/floating-context-widgets.tsx"
      provides: "Floating collapsible card widgets replacing fixed context panel sidebar"
      exports: ["FloatingContextWidgets"]
  key_links:
    - from: "src/components/session/wizard-shell.tsx"
      to: "src/components/session/wizard-step-sidebar.tsx"
      via: "Step sidebar rendered in wizard layout"
      pattern: "WizardStepSidebar"
    - from: "src/components/session/wizard-shell.tsx"
      to: "src/components/session/floating-context-widgets.tsx"
      via: "Floating widgets rendered alongside form content"
      pattern: "FloatingContextWidgets"
---

<objective>
Restructure the session wizard layout with left step sidebar, floating context widgets, and improved navigation placement.

Purpose: Better UX for the core product interaction -- running 1:1 sessions. Left step sidebar provides clear progress visibility, floating widgets give contextual information without stealing horizontal space, and repositioned buttons reduce mouse travel.

Output: Restructured wizard with vertical step navigation, floating context cards, slide transitions, and mobile-responsive layout.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-polish/10-CONTEXT.md
@.planning/phases/10-integration-polish/10-RESEARCH.md
@src/components/session/wizard-shell.tsx
@src/components/session/wizard-navigation.tsx
@src/components/session/wizard-top-bar.tsx
@src/components/session/context-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard step sidebar and floating context widgets</name>
  <files>
    src/components/session/wizard-step-sidebar.tsx,
    src/components/session/floating-context-widgets.tsx,
    src/components/session/wizard-shell.tsx,
    src/components/session/wizard-navigation.tsx,
    src/components/session/context-panel.tsx,
    src/app/globals.css
  </files>
  <action>
    **1. Create WizardStepSidebar (`wizard-step-sidebar.tsx`):**
    - Client component showing vertical list of all steps: Recap, each category name, Summary.
    - Each step shows: step number (or checkmark if complete), category name (truncated), completion indicator (e.g., "3/5 answered").
    - Active step highlighted with `bg-accent text-accent-foreground font-medium`.
    - Completed steps show checkmark icon with `bg-primary text-primary-foreground` circle.
    - Click on a step navigates to it (use existing `onStepChange` from wizard reducer).
    - Width: `w-[200px]` on desktop, hidden on mobile (`hidden md:flex`).
    - On mobile (< md): render as a horizontal scrollable strip at top of content area instead of vertical sidebar.
    - Fix scrollbar-over-navigation bug: ensure sidebar has `overflow-y: auto` with proper z-index, and navigation bar is not overlapped.

    **2. Create FloatingContextWidgets (`floating-context-widgets.tsx`):**
    - Transform the existing ContextPanel content into floating collapsible Card components.
    - Each context section becomes a standalone Card: "Previous Notes", "Open Action Items", "Score Trends", "AI Nudges".
    - Cards use shadcn Card component with CardHeader (collapsible trigger) and CardContent (collapsible body).
    - Cards float on the right side of the form content area. Use CSS: `absolute right-0 top-0` or `sticky top-20` within a right column.
    - Cards are collapsible individually (default: collapsed). User can expand to see details.
    - On desktop (lg+): cards render as a right column alongside the form (not overlapping).
    - On tablet (md-lg): cards hidden, accessible via a floating action button that opens a Sheet from the right.
    - On mobile (< md): cards stack below the form content. Add a "Context" button that scrolls to widgets or opens a bottom Sheet overlay (like iOS Maps).

    **3. Restructure wizard-shell.tsx layout:**
    - Remove the current layout structure (form area + fixed ContextPanel sidebar).
    - New layout:
      ```
      <div className="flex h-[calc(100vh-3.5rem)]">
        <WizardStepSidebar /> {/* left, 200px, hidden on mobile */}
        <div className="flex-1 overflow-y-auto relative">
          <div className="max-w-3xl mx-auto py-6 px-4">
            {/* Category step content with slide transition */}
            {/* Prev/Next buttons below form content */}
          </div>
        </div>
        <div className="hidden lg:block w-[280px] shrink-0 overflow-y-auto border-l p-4">
          <FloatingContextWidgets /> {/* right column on large screens */}
        </div>
      </div>
      ```
    - Move Prev/Next buttons from `WizardNavigation` bottom bar to inline below the form content (centered, within the form's max-width container). This positions them close to the last form input, addressing the "too far from Next button" UX issue.
    - Remove or repurpose `wizard-navigation.tsx` -- the bottom tab bar is replaced by the step sidebar. Keep the file if it contains reusable button logic, but remove the bottom bar rendering.

    **4. Add slide transitions between category steps:**
    - Use CSS transitions for slide animation when changing categories.
    - Apply `transition: transform 300ms ease-in-out` with `translateX` to slide content left/right.
    - Track transition direction (forward/backward) based on step index change.
    - If CSS transitions feel insufficient, this is the one case where framer-motion could be added (Claude's discretion per CONTEXT.md), but try CSS first.

    **5. Keep ContextPanel data logic:**
    - The data fetching and state management from `context-panel.tsx` should be preserved.
    - FloatingContextWidgets reuses the same data hooks/queries but renders in the new card layout.
    - If context-panel.tsx has reusable data logic, extract it into a hook (`useContextPanelData`) and import in FloatingContextWidgets.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run lint && bun run build</automated>
  </verify>
  <done>
    - Wizard renders with left step sidebar, center form, and right floating context widgets
    - Step sidebar shows all categories with completion indicators and checkmarks
    - Clicking a step navigates to that category
    - Prev/Next buttons are positioned below form content (not in a distant bottom bar)
    - Slide transitions animate between category steps
    - Mobile: step sidebar becomes horizontal strip, context accessible via bottom sheet
    - Tablet: context accessible via floating action button + right Sheet
    - Scrollbar-over-navigation bug is fixed
    - Build and typecheck pass
  </done>
</task>

</tasks>

<verification>
- `bun run build` succeeds
- Wizard layout renders correctly at desktop (1920px), laptop (1366px), tablet (1024px), and mobile (375px) widths
- All context panel data (notes, action items, score trends, nudges) accessible in new floating widget format
- Step navigation works via both sidebar clicks and Prev/Next buttons
</verification>

<success_criteria>
- Wizard layout matches the target design from RESEARCH.md
- Step sidebar provides clear progress visibility with completion indicators
- Context information is accessible without stealing form space
- Responsive across all common screen sizes
- No regression in wizard functionality (answers, auto-save, navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-polish/10-03-SUMMARY.md`
</output>
