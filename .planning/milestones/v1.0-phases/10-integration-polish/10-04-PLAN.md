---
phase: 10-integration-polish
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/app/globals.css
  - src/app/(dashboard)/overview/page.tsx
  - src/app/(dashboard)/sessions/page.tsx
  - src/app/(dashboard)/action-items/page.tsx
  - src/app/(dashboard)/history/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
  - src/app/(dashboard)/people/page.tsx
  - src/app/(dashboard)/teams/page.tsx
  - src/app/(dashboard)/settings/company/page.tsx
autonomous: true
requirements:
  - INFR-05

must_haves:
  truths:
    - "All screens have consistent spacing, typography, and visual hierarchy"
    - "Loading states use skeleton shimmer for all data-dependent sections"
    - "Empty states have descriptive messages and call-to-action buttons"
    - "All screens render correctly at desktop, tablet, and mobile widths"
    - "Subtle transitions (fade-in, hover states) are applied consistently"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Global transition utilities, skeleton shimmer animation, consistent spacing tokens"
      contains: "skeleton"
  key_links:
    - from: "src/app/(dashboard)/*/page.tsx"
      to: "src/app/globals.css"
      via: "Consistent Tailwind utility classes and CSS variables"
      pattern: "animate-"
---

<objective>
Responsive polish pass across all dashboard screens with consistent loading states, empty states, transitions, and spacing.

Purpose: Apple-level UI/UX polish -- the app should feel cohesive, responsive, and professional across all screen sizes. Every screen gets attention for loading states, empty states, and visual consistency.

Output: Polished, responsive screens with skeleton loading, descriptive empty states, subtle transitions, and consistent design language.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-polish/10-CONTEXT.md
@.planning/phases/10-integration-polish/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Visual audit and responsive polish across all screens</name>
  <files>
    src/app/globals.css,
    src/app/(dashboard)/overview/page.tsx,
    src/app/(dashboard)/sessions/page.tsx,
    src/app/(dashboard)/action-items/page.tsx,
    src/app/(dashboard)/history/page.tsx,
    src/app/(dashboard)/analytics/page.tsx,
    src/app/(dashboard)/people/page.tsx,
    src/app/(dashboard)/teams/page.tsx,
    src/app/(dashboard)/settings/company/page.tsx
  </files>
  <action>
    **1. Visual audit -- read every dashboard page and identify issues:**
    - Read each page component listed in files. Identify: hardcoded widths that break mobile, missing loading states, missing empty states, inconsistent spacing/padding, missing dark mode awareness.
    - Prioritize fixes by what looks worst (per Claude's discretion from CONTEXT.md).

    **2. Global CSS utilities:**
    - Add fade-in animation utility in globals.css: `@keyframes fade-in { from { opacity: 0; } to { opacity: 1; } }` with a `.animate-fade-in` class.
    - Ensure skeleton shimmer animation exists (shadcn Skeleton component likely handles this -- verify).
    - Add consistent page transition: wrap page content in a div with `animate-fade-in` for subtle page-load animation.

    **3. Loading states:**
    - For every page that fetches data (server or client), ensure a loading state exists.
    - Server Components: add `loading.tsx` files in route directories that show skeleton layouts matching the page structure.
    - Client Components with TanStack Query: ensure `isLoading` renders Skeleton components (not blank space or spinner-only).

    **4. Empty states:**
    - Every list/grid that can be empty should show a descriptive empty state with:
      - An icon or illustration (use lucide icons)
      - A message explaining what would appear here
      - A CTA button to create the first item (e.g., "Create your first template", "Start a session")
    - Screens to check: Sessions (no series), Action Items (no items), History (no sessions), Analytics (no data), People (no users beyond admin), Teams (no teams).

    **5. Responsive fixes:**
    - Test all page layouts mentally at 375px (mobile), 768px (tablet), 1024px (small laptop), 1366px (laptop), 1920px (desktop).
    - Fix any grid layouts that don't collapse to single column on mobile.
    - Fix any tables that overflow horizontally -- either make scrollable with `overflow-x-auto` or convert to card layout on mobile.
    - Ensure all modals/sheets are full-screen on mobile, drawer on tablet+.

    **6. Subtle transitions per locked decision:**
    - Add `transition-colors duration-200` to interactive elements (buttons, links, cards) for smooth hover states.
    - Cards: add `hover:shadow-md transition-shadow` for subtle lift effect on hover.
    - Page content: fade-in animation on mount.
    - Do NOT add complex animations, parallax, or attention-grabbing motion. Apple-like restraint.

    **7. Typography and spacing consistency:**
    - Page titles: consistent `text-2xl font-bold tracking-tight` or similar.
    - Section headings: consistent `text-lg font-semibold`.
    - Page padding: consistent `p-6` (or `px-4 py-6` on mobile).
    - Card spacing: consistent `gap-4` between cards in grids.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && bun run typecheck && bun run lint && bun run build</automated>
  </verify>
  <done>
    - All dashboard screens have skeleton loading states
    - All list/grid screens have descriptive empty states with CTAs
    - No horizontal overflow on mobile for any screen
    - Consistent page title typography and spacing across all screens
    - Subtle hover transitions on all interactive elements
    - Fade-in animation on page load
    - Dark mode renders correctly on all polished screens
    - Build passes
  </done>
</task>

</tasks>

<verification>
- `bun run build` succeeds
- No TypeScript errors
- Grep for TODO/FIXME in modified files returns zero results related to polish
</verification>

<success_criteria>
- Every dashboard screen looks polished and consistent
- Loading and empty states cover all data-dependent sections
- Responsive layout works across mobile, tablet, and desktop
- Dark mode works correctly on all screens
- Apple-like restraint in transitions -- subtle, not flashy
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-polish/10-04-SUMMARY.md`
</output>
