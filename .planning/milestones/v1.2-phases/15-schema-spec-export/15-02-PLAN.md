---
phase: 15-schema-spec-export
plan: "02"
type: execute
wave: 1
depends_on:
  - "15-01"
files_modified:
  - src/app/api/templates/[id]/export/route.ts
  - messages/en/spec.json
  - messages/ro/spec.json
  - src/app/(dashboard)/templates/schema/page.tsx
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
must_haves:
  truths:
    - "GET /api/templates/[id]/export returns a downloadable .json file with Content-Disposition: attachment"
    - "The export route returns 403 for member role and 401 for unauthenticated requests"
    - "The /templates/schema page renders the JSON schema block in English"
    - "The methodology and weight system sections render via t() from the spec namespace (not hardcoded English)"
    - "bun test passes (translation parity test passes for the new spec.json namespace)"
  artifacts:
    - path: src/app/api/templates/[id]/export/route.ts
      provides: GET handler returning tenant-neutral JSON with Content-Disposition attachment
      exports:
        - GET
    - path: messages/en/spec.json
      provides: English translations for spec page (pageTitle, tabs, schema, methodology, weights sections)
      contains: spec
    - path: messages/ro/spec.json
      provides: Romanian translations with identical keys to en/spec.json
      contains: spec
    - path: src/app/(dashboard)/templates/schema/page.tsx
      provides: Server Component rendering schema block + methodology + weights tabs
  key_links:
    - from: src/app/api/templates/[id]/export/route.ts
      to: src/lib/templates/export-schema.ts
      via: import buildExportPayload
      pattern: "buildExportPayload"
    - from: src/app/(dashboard)/templates/schema/page.tsx
      to: messages/en/spec.json
      via: getTranslations("spec")
      pattern: "getTranslations.*spec"
    - from: messages/en/spec.json
      to: messages/ro/spec.json
      via: translation parity CI test
      pattern: "spec\\.json"
---

<objective>
Build the export API route that serves a tenant-neutral .json file, and build the schema documentation page with three tabs (JSON Schema, Methodology, Score Weights) — all in content language via the new spec translation namespace.

Purpose: Delivers SPEC-01 (the downloadable JSON schema spec), SPEC-02 (methodology in content language), SPEC-03 (weight system docs in content language), and the API backbone for EXP-02 through EXP-05 (consumed by the export button in Plan 03).
Output: Export API route, spec.json translation files (en + ro), schema docs page.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-schema-spec-export/15-RESEARCH.md
@.planning/phases/15-schema-spec-export/15-01-SUMMARY.md

<interfaces>
<!-- Established patterns from existing codebase -->

From src/app/api/analytics/export/route.ts (Content-Disposition pattern):
```typescript
return new Response(csvContent, {
  headers: {
    "Content-Type": "text/csv",
    "Content-Disposition": `attachment; filename="1on1-analytics-${type}-${period}.csv"`,
  },
});
```

From src/app/api/templates/[id]/route.ts (auth + tenant context pattern):
```typescript
const session = await auth();
if (!session?.user) return NextResponse.json({ error: "Not authenticated" }, { status: 401 });
if (!canManageTemplates(session.user.role)) return NextResponse.json({ error: "Forbidden" }, { status: 403 });
const { id } = await params;
const result = await withTenantContext(session.user.tenantId, session.user.id, async (tx) => { ... });
```

From src/lib/templates/export-schema.ts (Plan 01 output):
```typescript
export const SCHEMA_VERSION = 1;
export interface TemplateExport { schemaVersion: 1; language: string; name: string; description: string | null; sections: ExportSection[]; }
export interface ExportSection { name: string; description: string | null; sortOrder: number; questions: ExportQuestion[]; }
export interface ExportQuestion { questionText: string; helpText: string | null; answerType: string; answerConfig: Record<string, unknown>; isRequired: boolean; sortOrder: number; scoreWeight: number; conditionalOnQuestionSortOrder: number | null; conditionalOperator: string | null; conditionalValue: string | null; }
export function buildExportPayload(template: unknown, contentLanguage: string): TemplateExport;
```

From src/types/next-auth.d.ts (contentLanguage in JWT):
session.user.contentLanguage is available in any auth()-gated route

Server Component i18n pattern (Phase 12+):
```typescript
import { getTranslations } from "next-intl/server";
const t = await getTranslations("spec");
// t("spec.pageTitle"), t("spec.tabs.schema"), etc.
```

Existing translation file structure (messages/en/templates.json):
Top-level key is the namespace name: { "templates": { ... } }
spec.json must follow same pattern: { "spec": { ... } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export API route GET /api/templates/[id]/export</name>
  <files>src/app/api/templates/[id]/export/route.ts</files>
  <behavior>
    - Test: GET with unauthenticated request → 401
    - Test: GET with member role → 403
    - Test: GET with admin/manager role for valid template → 200 with Content-Disposition: attachment header
    - Test: Response body parses as JSON matching TemplateExport shape (schemaVersion, language, name, sections)
    Note: These are integration-level behaviors; the unit tests in Plan 01 already cover buildExportPayload internals. Focus here on the route handler contract.
  </behavior>
  <action>
    Create src/app/api/templates/[id]/export/route.ts with a single GET export handler:

    1. Auth check: `const session = await auth()` — return 401 if no session.
    2. RBAC: `if (!canManageTemplates(session.user.role))` — return 403.
    3. Await params: `const { id } = await params` (RouteContext type identical to sibling route.ts).
    4. Fetch template with sections + questions using withTenantContext. Re-use the same query logic as GET /api/templates/[id] — fetch template row, then sections (non-archived, ordered by sortOrder), then questions (non-archived, ordered by sortOrder), group questions by sectionId into a Map, assemble the nested structure.
    5. If template not found, return 404.
    6. Call `buildExportPayload(templateWithSections, session.user.contentLanguage)` — imported from `@/lib/templates/export-schema`.
    7. Generate filename: `${template.name.toLowerCase().replace(/[^a-z0-9]+/g, '-')}-v${template.version}.json` — slug inline, no library.
    8. Return `new Response(JSON.stringify(exported, null, 2), { headers: { "Content-Type": "application/json", "Content-Disposition": \`attachment; filename="${filename}"\` } })`.

    Do NOT use NextResponse — use the bare Response constructor (matches analytics/export pattern).
    Do NOT log audit event — export is a read operation.
    Do NOT include any UUID fields: id, tenantId, createdBy, sectionId, templateId must not appear in the serialized JSON.
  </action>
  <verify>
    <automated>bun run typecheck && bun run lint</automated>
  </verify>
  <done>Route file exists; GET handler auth-gates with canManageTemplates; returns Content-Disposition: attachment; JSON output matches TemplateExport shape; no type errors</done>
</task>

<task type="auto">
  <name>Task 2: spec.json translation namespace (en + ro) and schema docs page</name>
  <files>
    messages/en/spec.json,
    messages/ro/spec.json,
    src/app/(dashboard)/templates/schema/page.tsx
  </files>
  <action>
    STEP A — Create messages/en/spec.json with complete content for all three tabs:

    ```json
    {
      "spec": {
        "pageTitle": "Template Schema & Documentation",
        "pageDescription": "Reference documentation for the 1on1 template JSON format, methodology principles, and score weight system.",
        "tabs": {
          "schema": "JSON Schema",
          "methodology": "Methodology",
          "weights": "Score Weights"
        },
        "schema": {
          "title": "Template JSON Schema",
          "description": "The canonical schema for template export and import. Always in English as a technical standard. Use this schema to validate templates created with external AI tools before importing.",
          "download": "Download Schema",
          "copy": "Copy",
          "copied": "Copied!",
          "schemaDocsLink": "View in template list"
        },
        "methodology": {
          "title": "1:1 Meeting Principles",
          "intro": "Effective 1:1 meetings follow a consistent structure that builds trust, surfaces blockers early, and creates accountability. These principles guide how templates should be designed.",
          "principles": {
            "continuity": {
              "title": "Continuity over completeness",
              "body": "A good template is used every session. Keep it short enough that both participants can complete it without rushing — 8 to 12 questions is a practical ceiling."
            },
            "specificity": {
              "title": "Specific questions surface real signal",
              "body": "Vague questions like \"How are things going?\" yield vague answers. Specific questions like \"What's the one thing blocking your progress this week?\" surface actionable information."
            },
            "balance": {
              "title": "Balance retrospective and prospective",
              "body": "Effective templates include both backward-looking questions (what happened, how do you feel about it) and forward-looking questions (what will you focus on, what do you need)."
            },
            "helpText": {
              "title": "Help text reduces ambiguity",
              "body": "Each question should have help text that explains what a good answer looks like. This is especially important for rating scales — anchor the low and high ends."
            }
          }
        },
        "weights": {
          "title": "Score Weight System",
          "intro": "Each question can carry a score weight that affects how much it contributes to the overall session score shown in analytics. The default weight is 1.",
          "validRange": "Valid range: 0 to 10",
          "defaultValue": "Default: 1",
          "zeroMeaning": "Weight 0 means the question is unscored — it appears in the session but does not affect analytics.",
          "examples": {
            "title": "Worked examples",
            "basic": "A template with three rating questions all weighted 1 contributes equally to the session score.",
            "emphasized": "Increase weight to 2 or 3 for questions that signal high-importance outcomes — for example, a \"blockers\" question or an overall satisfaction rating.",
            "unscored": "Set weight to 0 for open-ended text questions where a numeric score doesn't make sense, such as \"What's on your mind this week?\""
          }
        }
      }
    }
    ```

    STEP B — Create messages/ro/spec.json with identical structure and Romanian translations. All keys must be present with the same nesting — the translation parity CI test (bun test) will fail if any key is missing or extra.

    Romanian content for the spec namespace:
    ```json
    {
      "spec": {
        "pageTitle": "Schema și documentație șabloane",
        "pageDescription": "Documentație de referință pentru formatul JSON al șabloanelor 1:1, principii metodologice și sistemul de ponderi pentru scoruri.",
        "tabs": {
          "schema": "Schema JSON",
          "methodology": "Metodologie",
          "weights": "Ponderi scoruri"
        },
        "schema": {
          "title": "Schema JSON pentru șabloane",
          "description": "Schema canonică pentru exportul și importul șabloanelor. Mereu în engleză ca standard tehnic. Utilizați această schemă pentru a valida șabloanele create cu instrumente AI externe înainte de import.",
          "download": "Descarcă schema",
          "copy": "Copiază",
          "copied": "Copiat!",
          "schemaDocsLink": "Vezi în lista de șabloane"
        },
        "methodology": {
          "title": "Principii pentru ședințele 1:1",
          "intro": "Ședințele 1:1 eficiente urmează o structură consistentă care construiește încredere, identifică blocajele timpuriu și creează responsabilitate. Aceste principii ghidează modul în care ar trebui concepute șabloanele.",
          "principles": {
            "continuity": {
              "title": "Continuitate în locul completitudinii",
              "body": "Un șablon bun este folosit la fiecare ședință. Păstrați-l suficient de scurt pentru ca ambii participanți să îl poată completa fără grabă — 8 până la 12 întrebări este un plafon practic."
            },
            "specificity": {
              "title": "Întrebările specifice generează semnale reale",
              "body": "Întrebările vagi precum „Cum merg lucrurile?" duc la răspunsuri vagi. Întrebările specifice precum „Care este singurul lucru care îți blochează progresul săptămâna aceasta?" generează informații acționabile."
            },
            "balance": {
              "title": "Echilibru între retrospectiv și prospectiv",
              "body": "Șabloanele eficiente includ atât întrebări retrospective (ce s-a întâmplat, cum te simți în legătură cu asta), cât și întrebări prospective (pe ce te vei concentra, de ce ai nevoie)."
            },
            "helpText": {
              "title": "Textul de ajutor reduce ambiguitatea",
              "body": "Fiecare întrebare ar trebui să aibă un text de ajutor care explică cum arată un răspuns bun. Acest lucru este deosebit de important pentru scalele de evaluare — ancorați capetele inferior și superior."
            }
          }
        },
        "weights": {
          "title": "Sistemul de ponderi pentru scoruri",
          "intro": "Fiecare întrebare poate purta o pondere care afectează cât de mult contribuie la scorul general al ședinței afișat în analize. Ponderea implicită este 1.",
          "validRange": "Interval valid: 0 până la 10",
          "defaultValue": "Implicit: 1",
          "zeroMeaning": "Ponderea 0 înseamnă că întrebarea nu este punctată — apare în ședință, dar nu afectează analizele.",
          "examples": {
            "title": "Exemple de calcul",
            "basic": "Un șablon cu trei întrebări de evaluare toate ponderate cu 1 contribuie în mod egal la scorul ședinței.",
            "emphasized": "Măriți ponderea la 2 sau 3 pentru întrebările care semnalează rezultate importante — de exemplu, o întrebare despre blocaje sau o evaluare generală a satisfacției.",
            "unscored": "Setați ponderea la 0 pentru întrebările text cu răspuns liber unde un scor numeric nu are sens, cum ar fi „Ce îți trece prin minte săptămâna aceasta?""
          }
        }
      }
    }
    ```

    STEP C — Create src/app/(dashboard)/templates/schema/page.tsx as a Server Component:

    - Import getTranslations from "next-intl/server" and auth from "@/lib/auth/config"
    - Redirect unauthenticated users (auth() check; if no session redirect to /login)
    - Use shadcn/ui Tabs (TabsList, TabsTrigger, TabsContent) for the three sections
    - Tab 1 "JSON Schema": Render the JSON schema as a `<pre><code>` block (hardcoded English JSON string from RESEARCH.md Pattern 2). Include two buttons: "Download Schema" (client interaction — use a small inline Client Component or an anchor tag with data-uri for the JSON blob) and "Copy" (navigator.clipboard.writeText). Show toast on copy success using t("spec.schema.copied").
    - Tab 2 "Methodology": Render t("spec.methodology.title"), t("spec.methodology.intro"), then four principle cards each with title and body from t("spec.methodology.principles.*").
    - Tab 3 "Score Weights": Render t("spec.weights.title"), t("spec.weights.intro"), valid range, default value, zero meaning, and examples section.
    - Page title: t("spec.pageTitle"), page description: t("spec.pageDescription").
    - Add a "Back to Templates" link (href="/templates") with ArrowLeft icon (same pattern as template editor's backToTemplates button).
    - The copy and download buttons require "use client" — extract them as a small SchemaActions client component in the same file or a sibling file src/app/(dashboard)/templates/schema/schema-actions.tsx.

    The JSON schema string to embed (static, hardcoded English):
    Use the full JSON Schema draft-07 object from RESEARCH.md Code Examples section — include $schema, title, version, type, required, and properties for schemaVersion, language, name, description, sections (with full nested items definition).
  </action>
  <verify>
    <automated>bun test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    - messages/en/spec.json and messages/ro/spec.json exist with identical key structure
    - bun test passes (translation parity test covers new spec.json)
    - /templates/schema page renders without type errors
    - Three tabs present: JSON Schema (hardcoded English schema block), Methodology (t() keys), Score Weights (t() keys)
    - No hardcoded English strings in methodology or weights sections — all go through t()
  </done>
</task>

</tasks>

<verification>
Overall phase checks for Plans 01 + 02:
- `bun run typecheck` — zero errors across new files
- `bun run lint` — zero warnings
- `bun test` — translation parity test passes (spec.json en + ro key parity), export-schema unit tests pass
- GET /api/templates/[id]/export returns Content-Disposition: attachment; filename="*.json"
- JSON output has schemaVersion: 1, language matches session contentLanguage, no UUID patterns
</verification>

<success_criteria>
- Export API route exists and gate-keeps with canManageTemplates()
- spec.json exists in both en/ and ro/ with identical key counts
- /templates/schema page renders all three tabs using getTranslations("spec")
- bun test green (parity + unit tests)
- No type errors, no lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-schema-spec-export/15-02-SUMMARY.md`
</output>
