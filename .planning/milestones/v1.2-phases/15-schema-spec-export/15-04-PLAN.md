---
phase: 15-schema-spec-export
plan: "04"
type: execute
wave: 3
depends_on:
  - "15-03"
files_modified: []
autonomous: false
requirements:
  - EXP-01
must_haves:
  truths:
    - "A member user sees no Export button in the template list or editor"
    - "An admin or manager sees the Export button in both template list and editor"
    - "Clicking Export downloads a .json file to disk"
    - "The /templates/schema page loads and all three tabs render correctly"
    - "The methodology and weights tabs display content in the company content language (not hardcoded English)"
  artifacts:
    - path: src/components/templates/export-button.tsx
      provides: Confirmed working in browser (not just typecheck)
    - path: src/app/(dashboard)/templates/schema/page.tsx
      provides: Confirmed rendering in browser with all three tabs
  key_links:
    - from: browser export button click
      to: /api/templates/[id]/export
      via: fetch + blob download
      pattern: "Content-Disposition"
---

<objective>
Manual role-gating verification for EXP-01. Confirms that the export button is visible to admin/manager but hidden from member, and that the actual download produces a valid .json file. Also verifies the schema docs page renders correctly in browser.

Purpose: EXP-01 cannot be verified by automated tests (requires UI role-switching). This checkpoint ensures the RBAC conditional rendering works as intended before the phase is marked complete.
Output: No new files. Phase 15 is verified complete.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-schema-spec-export/15-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - Export API route: GET /api/templates/[id]/export (auth-gated, tenant-neutral JSON download)
    - Schema docs page: /templates/schema (three tabs: JSON Schema, Methodology, Score Weights)
    - ExportButton component wired into template list (icon) and editor toolbar (full)
    - Translation keys for export actions in both en and ro
    - spec.json translation namespace with full methodology and weight system content
  </what-built>
  <how-to-verify>
    1. Start the dev server: `bun run dev` (or check it is running on port 4300)

    2. TEST — Export button visibility (EXP-01 role gating):
       a. Log in as a MEMBER user
       b. Navigate to /templates
       c. Verify: NO Export button visible on any template card
       d. Open any template in the editor (/templates/[id])
       e. Verify: NO Export button in the editor toolbar
       f. Log out, log in as ADMIN or MANAGER
       g. Navigate to /templates
       h. Verify: Export button (download icon) IS visible on template cards
       i. Open a template in the editor
       j. Verify: "Export" button IS visible in the toolbar

    3. TEST — Export download (EXP-02 through EXP-05):
       a. As admin/manager, click the Export button on any template
       b. Verify: A .json file downloads (check Downloads folder or browser download bar)
       c. Open the downloaded file and verify:
          - `"schemaVersion": 1` is present (integer, not string)
          - `"language": "en"` (or "ro" if company content language is Romanian)
          - No UUID-pattern fields (no id, tenantId, sectionId, conditionalOnQuestionId)
          - `scoreWeight` values are numbers like `1` not strings like `"1.00"`
          - `answerConfig` is an object (even `{}` for text questions)
          - If any question has conditional logic: `conditionalOnQuestionSortOrder` is an integer

    4. TEST — Schema docs page (SPEC-01, SPEC-02, SPEC-03):
       a. Navigate to /templates/schema (or click "Schema Docs" link in the templates header)
       b. Verify: Page loads with three tabs: JSON Schema, Methodology, Score Weights
       c. Click "JSON Schema" tab — verify: JSON code block is visible and in English
       d. Click "Download Schema" — verify: A .json file downloads containing the schema definition
       e. Click "Copy" — verify: Clipboard receives the schema JSON (paste into text editor to confirm)
       f. Click "Methodology" tab — verify: Content renders in the company content language (Romanian if RO, English if EN) — not hardcoded English regardless of language setting
       g. Click "Score Weights" tab — verify: Content renders in content language with worked examples
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 15 complete, or describe specific issues for gap closure</resume-signal>
</task>

</tasks>

<verification>
Phase 15 is verified complete when all role-gating and download behaviors are confirmed correct by the user.
</verification>

<success_criteria>
- Member: no Export button visible anywhere
- Admin/manager: Export button in template list AND editor
- Export download: valid tenant-neutral JSON with schemaVersion, language, no UUIDs, numeric scoreWeight
- /templates/schema: all three tabs render, methodology and weights use content language, schema block in English
- Download schema and Copy buttons work in the schema docs page
</success_criteria>

<output>
After approval, create `.planning/phases/15-schema-spec-export/15-04-SUMMARY.md`
Update ROADMAP.md Phase 15 Plans field: `**Plans:** 4 plans`
</output>
