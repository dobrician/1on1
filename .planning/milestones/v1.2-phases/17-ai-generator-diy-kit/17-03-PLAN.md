---
phase: 17-ai-generator-diy-kit
plan: 03
type: execute
wave: 1
depends_on:
  - 17-01
files_modified:
  - messages/en/spec.json
  - messages/ro/spec.json
  - messages/en/templates.json
  - messages/ro/templates.json
autonomous: true
requirements:
  - DIY-01
  - DIY-02
  - AIGEN-01
  - AIGEN-02

must_haves:
  truths:
    - "spec.tabs.promptKit key exists in both EN and RO translation files"
    - "spec.promptKit.* keys cover: intro, copy, copied, all 4 section labels"
    - "templates.aiEditor.* keys cover: page title, header actions, chat UI, save/reset/back, empty state"
    - "EN and RO key sets are identical (CI key parity check passes)"
  artifacts:
    - path: "messages/en/spec.json"
      provides: "promptKit.* translation keys for DIY tab"
    - path: "messages/ro/spec.json"
      provides: "Romanian translations of all promptKit.* keys"
    - path: "messages/en/templates.json"
      provides: "aiEditor.* translation keys for AI editor pages"
    - path: "messages/ro/templates.json"
      provides: "Romanian translations of all aiEditor.* keys"
  key_links:
    - from: "src/app/(dashboard)/templates/schema/page.tsx"
      to: "messages/en/spec.json"
      via: "t('spec.tabs.promptKit') and t('spec.promptKit.*')"
      pattern: "spec\\.promptKit"
    - from: "src/app/(dashboard)/templates/ai-editor/page.tsx"
      to: "messages/en/templates.json"
      via: "t('templates.aiEditor.*')"
      pattern: "templates\\.aiEditor"
---

<objective>
Add all translation keys required for the AI editor pages and the DIY Prompt Kit tab. Both EN and RO.

Purpose: Translation keys must exist before pages reference them. Creating them as a dedicated Wave 1 plan (parallel with 17-02) ensures no placeholder strings ship.
Output: 4 JSON files updated. All new UI strings available in both languages.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-ai-generator-diy-kit/17-CONTEXT.md

<interfaces>
<!-- Existing spec.json structure to extend -->

Current messages/en/spec.json top-level structure:
```json
{
  "spec": {
    "pageTitle": "...",
    "pageDescription": "...",
    "tabs": { "schema": "...", "methodology": "...", "weights": "..." },
    "schema": { ... },
    "methodology": { ... },
    "weights": { ... }
  }
}
```

Key i18n patterns used in this codebase:
- Server components: getTranslations() without namespace, then t('namespace.key')
- Client components: useTranslations('namespace'), then t('key')
- New keys must be added to BOTH en and ro files — CI parity check enforces this
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add promptKit keys to spec.json (EN + RO)</name>
  <files>
    messages/en/spec.json
    messages/ro/spec.json
  </files>
  <action>
    Read `messages/en/spec.json` and `messages/ro/spec.json`.

    **messages/en/spec.json** — add two things:

    1. Add `"promptKit"` to `spec.tabs`:
    ```json
    "tabs": {
      "schema": "JSON Schema",
      "methodology": "Methodology",
      "weights": "Score Weights",
      "promptKit": "Prompt Kit"
    }
    ```

    2. Add `spec.promptKit` object at the same level as `spec.schema`, `spec.methodology`, `spec.weights`:
    ```json
    "promptKit": {
      "title": "DIY Prompt Kit",
      "intro": "Paste this into Claude, ChatGPT, or any AI assistant to generate a 1on1 template tailored to your team.",
      "copy": "Copy Prompt Kit",
      "copied": "Copied!",
      "sections": {
        "schema": "JSON Schema Spec",
        "methodology": "1:1 Methodology Principles",
        "weights": "Score Weight System",
        "example": "Worked Example Template"
      }
    }
    ```

    **messages/ro/spec.json** — add equivalent Romanian translations:
    ```json
    "tabs": {
      ...existing...,
      "promptKit": "Kit de Prompt"
    },
    "promptKit": {
      "title": "Kit de Prompt DIY",
      "intro": "Lipește aceasta în Claude, ChatGPT sau orice asistent AI pentru a genera un șablon de 1:1 adaptat echipei tale.",
      "copy": "Copiază Kitul de Prompt",
      "copied": "Copiat!",
      "sections": {
        "schema": "Specificația JSON Schema",
        "methodology": "Principiile Metodologiei 1:1",
        "weights": "Sistemul de Ponderi",
        "example": "Șablon Exemplu Complet"
      }
    }
    ```

    Write the updated files. Preserve all existing keys exactly.
  </action>
  <verify>
    <automated>npx vitest run src/lib/i18n/__tests__/key-parity.test.ts 2>/dev/null || npx vitest run --reporter=verbose 2>&1 | grep -E "parity|PASS|FAIL" | head -10</automated>
  </verify>
  <done>Both spec.json files updated with identical key structure. Key parity CI test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add aiEditor keys to templates.json (EN + RO)</name>
  <files>
    messages/en/templates.json
    messages/ro/templates.json
  </files>
  <action>
    Read `messages/en/templates.json` and `messages/ro/templates.json`. Add `aiEditor` object to both.

    **messages/en/templates.json** — add under the top-level `templates` object:
    ```json
    "aiEditor": {
      "pageTitle": "AI Template Editor",
      "header": {
        "newTemplate": "New Template",
        "editTemplate": "Edit Template",
        "save": "Save Template",
        "reset": "Reset",
        "back": "Back to Templates",
        "backToBuilder": "Back to Builder"
      },
      "resetConfirm": {
        "title": "Start over?",
        "description": "This will clear the template and conversation. This cannot be undone.",
        "confirm": "Start Over",
        "cancel": "Cancel"
      },
      "preview": {
        "title": "Template Preview",
        "empty": "Your template will appear here as you chat",
        "sectionCount": "{count, plural, one {# section} other {# sections}}",
        "questionCount": "{count, plural, one {# question} other {# questions}}",
        "answerTypeLabel": {
          "text": "Text",
          "rating_1_5": "Rating 1–5",
          "rating_1_10": "Rating 1–10",
          "yes_no": "Yes / No",
          "multiple_choice": "Multiple Choice",
          "mood": "Mood",
          "scale_custom": "Custom Scale"
        }
      },
      "chat": {
        "placeholder": "Describe your team, goals, or ask for changes...",
        "send": "Send",
        "thinking": "Thinking...",
        "errorToast": "AI generation failed. Please try again."
      },
      "saveSuccess": "Template saved",
      "entryPoints": {
        "generateWithAI": "Generate with AI",
        "editWithAI": "Edit with AI"
      }
    }
    ```

    **messages/ro/templates.json** — add equivalent Romanian:
    ```json
    "aiEditor": {
      "pageTitle": "Editor AI de Șabloane",
      "header": {
        "newTemplate": "Șablon Nou",
        "editTemplate": "Editează Șablonul",
        "save": "Salvează Șablonul",
        "reset": "Resetează",
        "back": "Înapoi la Șabloane",
        "backToBuilder": "Înapoi la Constructor"
      },
      "resetConfirm": {
        "title": "Începi de la capăt?",
        "description": "Aceasta va șterge șablonul și conversația. Nu poate fi anulat.",
        "confirm": "Începe de la Capăt",
        "cancel": "Anulează"
      },
      "preview": {
        "title": "Previzualizare Șablon",
        "empty": "Șablonul tău va apărea aici pe măsură ce conversezi",
        "sectionCount": "{count, plural, one {# secțiune} few {# secțiuni} other {# secțiuni}}",
        "questionCount": "{count, plural, one {# întrebare} few {# întrebări} other {# întrebări}}",
        "answerTypeLabel": {
          "text": "Text",
          "rating_1_5": "Evaluare 1–5",
          "rating_1_10": "Evaluare 1–10",
          "yes_no": "Da / Nu",
          "multiple_choice": "Alegere Multiplă",
          "mood": "Stare",
          "scale_custom": "Scară Personalizată"
        }
      },
      "chat": {
        "placeholder": "Descrie echipa ta, obiectivele sau cere modificări...",
        "send": "Trimite",
        "thinking": "Se gândește...",
        "errorToast": "Generarea AI a eșuat. Încearcă din nou."
      },
      "saveSuccess": "Șablon salvat",
      "entryPoints": {
        "generateWithAI": "Generează cu AI",
        "editWithAI": "Editează cu AI"
      }
    }
    ```

    Write both files. Preserve all existing keys exactly (read first to get current content).
  </action>
  <verify>
    <automated>npx vitest run src/lib/i18n/__tests__/key-parity.test.ts 2>/dev/null || npx vitest run --reporter=verbose 2>&1 | grep -E "parity|PASS|FAIL" | head -10</automated>
  </verify>
  <done>Both templates.json files updated with identical aiEditor key structure. Key parity CI test passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass including key parity (no new missing keys)
- `npx tsc --noEmit` — no type errors
- Both spec.json files have `tabs.promptKit` and `promptKit.*` keys
- Both templates.json files have `aiEditor.*` keys
- EN and RO key structures are exactly mirrored
</verification>

<success_criteria>
- 4 translation files updated: en/spec.json, ro/spec.json, en/templates.json, ro/templates.json
- Zero key parity failures — EN and RO sets match exactly
- Romanian uses correct diacritics (comma-below: ș/ț not cedilla variants)
- Romanian plural forms use three-form ICU (one/few/other) for count keys
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-generator-diy-kit/17-03-SUMMARY.md`
</output>
