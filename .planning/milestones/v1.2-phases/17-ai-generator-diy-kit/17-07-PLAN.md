---
phase: 17-ai-generator-diy-kit
plan: 07
type: execute
wave: 4
depends_on:
  - 17-05
  - 17-06
files_modified: []
autonomous: false
requirements:
  - AIGEN-01
  - AIGEN-02
  - AIGEN-03
  - AIGEN-04
  - DIY-01
  - DIY-02

must_haves:
  truths:
    - "Admin or manager can navigate to /templates/ai-editor via the Generate with AI button"
    - "AI sends a greeting question automatically when the editor opens"
    - "Describing a team produces a complete template draft in the preview panel"
    - "Generated template is in the company's content language"
    - "Save creates the template in the DB and a success toast appears"
    - "Reset clears both panels after confirmation"
    - "Edit with AI opens an existing template pre-loaded in the preview"
    - "DIY Prompt Kit tab is visible on /templates/schema with a working Copy button"
    - "Member role cannot access /templates/ai-editor (redirected)"
  artifacts:
    - path: "src/app/(dashboard)/templates/ai-editor/page.tsx"
      provides: "Verified: accessible, RBAC gated, AI greeting fires"
    - path: "src/app/(dashboard)/templates/schema/page.tsx"
      provides: "Verified: 4th Prompt Kit tab exists, copy works"
  key_links:
    - from: "template-list.tsx Generate with AI button"
      to: "/templates/ai-editor page"
      via: "navigation"
      pattern: "ai-editor"
    - from: "AI chat turn"
      to: "left preview panel"
      via: "templateJson parsed and re-rendered"
      pattern: "templateJson"
---

<objective>
Manual verification of the complete AI Generator and DIY Kit feature: confirm the end-to-end chat flow, template generation, save, reset, and the DIY prompt kit copy flow all work correctly in the browser.

Purpose: The AI chat interaction and clipboard copy cannot be verified with unit tests alone — they require a real browser, a real AI response, and actual clipboard state. This checkpoint confirms Phase 17 is complete before the milestone is closed.
Output: User confirms all 7 scenarios pass. Phase 17 marked complete.

**Pre-flight (run before presenting checkpoint):**
```
npx vitest run
npx tsc --noEmit
npm run build
```
All three must exit 0 before the checkpoint proceeds. Fix any failures first.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-ai-generator-diy-kit/17-CONTEXT.md
@.planning/phases/17-ai-generator-diy-kit/17-05-SUMMARY.md
@.planning/phases/17-ai-generator-diy-kit/17-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 17 complete implementation:
    - AI Template Editor: full-page split-screen at /templates/ai-editor (new) and /templates/[id]/ai-editor (existing)
    - Entry points: "Generate with AI" button (Wand2) in template list header; "Edit with AI" button (Wand2) in template builder header
    - AI chat: greeting fires on load, preview updates on every AI turn that returns templateJson
    - Save/Reset flow with confirmation dialog; RBAC gating (admin + manager only)
    - DIY Prompt Kit: 4th tab on /templates/schema — copyable block with JSON schema + methodology + weights + worked example
    - All narrative content in company's content language; JSON schema and field names always in English
  </what-built>
  <how-to-verify>
    Start dev server if not running: `bun run dev --port 4300`
    Open: https://1on1.surmont.co/templates

    **Scenario 1 — Entry point + AI greeting (AIGEN-01)**
    1. Log in as admin or manager, navigate to /templates
    2. Verify "Generate with AI" button (Wand icon) appears in the template list header
    3. Click "Generate with AI" → verify full-page split-screen opens at /templates/ai-editor
    4. Verify: within a few seconds, the chat panel shows an AI greeting question about team and goals
    5. Verify: the left preview panel shows empty state ("Your template will appear here as you chat")

    **Scenario 2 — Template generation + preview (AIGEN-02)**
    1. In the AI editor, type: "Engineering team of 5, biweekly 1:1s, 45 minutes" → send
    2. Wait for AI response
    3. Verify: the left preview panel updates with a template (section headers + question cards)
    4. Verify: the Save button becomes enabled

    **Scenario 3 — Content language (AIGEN-03)**
    1. In Company Settings, set content language to Romanian
    2. Repeat Scenario 2 in a new AI editor session
    3. Verify: all question text, help text, and section names are in Romanian
    4. Set language back to English after verifying

    **Scenario 4 — Save (AIGEN-02)**
    1. After template appears in preview (Scenario 2), click Save
    2. Verify: "Template saved" success toast appears; editor stays open (no redirect)
    3. Navigate to /templates → verify the new template appears in the list

    **Scenario 5 — Reset (AIGEN-01)**
    1. In the AI editor with an active template, click Reset
    2. Verify: confirmation dialog appears ("Start over?")
    3. Click Cancel → verify nothing changes
    4. Click Reset again → confirm → verify both preview (empty state) and chat are cleared

    **Scenario 6 — Edit with AI (AIGEN-01)**
    1. Open any template in /templates/[id] (template builder)
    2. Verify: "Edit with AI" button (Wand icon) appears in the header
    3. Click Edit with AI → verify /templates/[id]/ai-editor opens with existing template already in preview
    4. Verify: AI sends an opening message summarizing the existing template

    **Scenario 7 — DIY Prompt Kit (DIY-01, DIY-02)**
    1. Navigate to /templates/schema
    2. Verify: 4th tab "Prompt Kit" exists in the tab bar
    3. Click Prompt Kit → verify intro sentence visible ("Paste this into Claude, ChatGPT...")
    4. Verify: copyable block shows all 4 sections (JSON schema + methodology + weights + example)
    5. Click "Copy Prompt Kit" → paste into a text editor → verify all 4 sections present
    6. Verify: methodology, weights, and example narrative are in company's content language (English by default)
    7. Verify: JSON field names in schema and example template are in English

    **RBAC check**
    1. Log in as a member → navigate to /templates → verify no "Generate with AI" button
    2. Visit /templates/ai-editor directly as member → verify redirect to /templates
  </how-to-verify>
  <resume-signal>Type "approved" after all 7 scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 7 scenarios pass as confirmed by user. Phase 17 is complete.
</verification>

<success_criteria>
- AIGEN-01: Admin/manager can open AI editor, describe their team, receive a template draft
- AIGEN-02: Template appears in preview before saving; user can accept (save), discard (reset), or refine (keep chatting)
- AIGEN-03: All generated content (question text, help text, section names) is in company's content language
- AIGEN-04: AI uses schema spec, methodology principles, and weight system in its generation (confirmed by quality of output)
- DIY-01: User can access and copy prompt kit block from /templates/schema Prompt Kit tab
- DIY-02: Prompt kit narrative and section labels are in company's content language; JSON schema and field names remain in English
</success_criteria>

<output>
After user approves, create `.planning/phases/17-ai-generator-diy-kit/17-07-SUMMARY.md` marking Phase 17 complete.
</output>
