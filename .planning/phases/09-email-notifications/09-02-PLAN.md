---
phase: 09-email-notifications
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/ai/pipeline.ts
  - src/lib/notifications/summary-email.ts
  - src/app/api/sessions/[id]/complete/route.ts
  - src/app/(dashboard)/settings/company/page.tsx
  - src/lib/ai/service.ts
  - src/lib/db/seed.ts
  - src/app/api/series/route.ts
  - src/app/api/series/[id]/route.ts
autonomous: true
requirements:
  - NOTF-03
  - NOTF-02
  - NOTF-04

must_haves:
  truths:
    - "After AI pipeline completes, both manager and report receive a summary email with session content and AI insights"
    - "Manager's summary email includes the AI addendum section (sentiment, coaching suggestions)"
    - "Report's summary email has no addendum -- shared content only"
    - "When a session is completed and nextSessionAt is computed, pre-meeting and agenda prep notifications are scheduled for the next session"
    - "When a series reminder setting changes, old pending notifications are cancelled and new ones are created"
    - "Organization admin can set a preferred language in company settings"
    - "AI prompts include the org's preferred language instruction"
  artifacts:
    - path: "src/lib/notifications/summary-email.ts"
      provides: "Post-session summary email sender (event-driven, not cron)"
      exports: ["sendPostSessionSummaryEmails"]
    - path: "src/lib/ai/pipeline.ts"
      provides: "AI pipeline with summary email trigger after completion"
      contains: "sendPostSessionSummaryEmails"
    - path: "src/app/api/sessions/[id]/complete/route.ts"
      provides: "Session completion with notification scheduling for next session"
      contains: "scheduleSeriesNotifications"
  key_links:
    - from: "src/lib/ai/pipeline.ts"
      to: "src/lib/notifications/summary-email.ts"
      via: "sendPostSessionSummaryEmails call after aiStatus=completed"
      pattern: "sendPostSessionSummaryEmails"
    - from: "src/app/api/sessions/[id]/complete/route.ts"
      to: "src/lib/notifications/scheduler.ts"
      via: "scheduleSeriesNotifications after nextSessionAt computed"
      pattern: "scheduleSeriesNotifications"
    - from: "src/lib/ai/service.ts"
      to: "tenants.settings.preferredLanguage"
      via: "language instruction in AI prompts"
      pattern: "preferredLanguage|Respond in"
---

<objective>
Wire email notifications into the application lifecycle: post-session summary emails triggered by AI pipeline completion, notification scheduling on session completion, org language setting for AI content, and series reminder configuration.

Purpose: Connect the email infrastructure from Plan 01 to actual application events. After this plan, emails fire automatically at the right moments -- summary after AI completes, reminders before next meeting, agenda prep 48h out.

Output: Event-driven summary email sender, pipeline integration, completion endpoint scheduling, org language setting UI and AI integration.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-email-notifications/09-CONTEXT.md
@.planning/phases/09-email-notifications/09-RESEARCH.md
@.planning/phases/09-email-notifications/09-01-SUMMARY.md

@src/lib/ai/pipeline.ts
@src/app/api/sessions/[id]/complete/route.ts
@src/lib/db/schema/notifications.ts
@src/lib/db/schema/series.ts
@src/lib/db/schema/tenants.ts
@src/lib/db/schema/sessions.ts
@src/app/(dashboard)/settings/company/page.tsx

<interfaces>
<!-- From Plan 01 (expected outputs) -->
From src/lib/email/send.ts:
  export function getTransport(): nodemailer.Transporter;
  export function getEmailFrom(): string;

From src/lib/notifications/scheduler.ts:
  export function scheduleSeriesNotifications(params: {
    tenantId: string; seriesId: string; managerId: string; reportId: string;
    nextSessionAt: Date; reminderHoursBefore: number;
    managerName: string; reportName: string;
  }): Promise<void>;

From src/lib/notifications/queries.ts:
  export function cancelSeriesNotifications(seriesId: string, types?: string[]): Promise<void>;

From src/lib/email/templates/session-summary.tsx:
  export function SessionSummaryEmail(props: {
    variant: 'manager' | 'report';
    recipientName: string; otherPartyName: string;
    sessionNumber: number; sessionScore: number | null;
    aiSummary: { overallSentiment: string; keyTakeaways: string[]; areasOfConcern: string[] } | null;
    actionItems: { title: string; assigneeName: string; dueDate: string | null }[];
    viewSessionUrl: string;
    aiAddendum?: { sentimentAnalysis: string; coachingSuggestions: string[]; riskIndicators: string[] } | null;
  }): JSX.Element;

<!-- Existing AI pipeline interface (from src/lib/ai/pipeline.ts) -->
export async function runAIPipelineDirect(input: {
  sessionId: string; seriesId: string; tenantId: string;
  managerId: string; reportId: string;
}): Promise<void>;

<!-- Session completion return value (from complete/route.ts) -->
Result includes: sessionId, score, nextSessionAt, seriesId, managerId, reportId
Series record includes: reminderHoursBefore (new from Plan 01)

<!-- Tenant settings JSONB structure -->
tenants.settings: jsonb -- add preferredLanguage?: string (e.g., "en", "ro", "de")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Post-session summary email sender and AI pipeline integration</name>
  <files>
    src/lib/notifications/summary-email.ts
    src/lib/ai/pipeline.ts
    src/lib/ai/service.ts
    src/app/(dashboard)/settings/company/page.tsx
    src/app/api/settings/company/route.ts
  </files>
  <action>
    1. **Create `src/lib/notifications/summary-email.ts`** -- event-driven summary email sender (NOT cron-scheduled per research recommendation):
       - `sendPostSessionSummaryEmails(params: { sessionId, seriesId, tenantId, managerId, reportId })`:
         a. Fetch session data (sessionNumber, sessionScore, aiSummary, aiManagerAddendum, aiSuggestions) using adminDb
         b. Fetch series + manager user + report user (names, emails) using adminDb
         c. Fetch action items created in this session (from action_items table WHERE sourceSessionId = sessionId)
         d. Build `viewSessionUrl` = `${process.env.NEXT_PUBLIC_APP_URL}/sessions/${sessionId}/summary`
         e. Render SessionSummaryEmail for report (variant='report', no addendum)
         f. Render SessionSummaryEmail for manager (variant='manager', with aiAddendum)
         g. Send both emails via getTransport().sendMail() -- non-blocking, log errors but don't throw
         h. Optionally insert 2 notification records with status='sent' for audit trail (type='session_summary', channel='email', referenceType='session', referenceId=sessionId)
       - Handle AI failure gracefully: if aiSummary is null (aiStatus was 'failed'), still send the email with answers/action items/score, just without the AI sections. Per locked decision we wait for AI, but the pipeline sets aiStatus='failed' on error and we should still send what we have.

    2. **Wire into AI pipeline** (`src/lib/ai/pipeline.ts`):
       - Import sendPostSessionSummaryEmails from `@/lib/notifications/summary-email`
       - After the final `aiStatus: "completed"` update (line ~118) and after the analytics snapshot try/catch block (line ~147), add:
         ```typescript
         // Send post-session summary emails (non-fatal)
         try {
           await sendPostSessionSummaryEmails({
             sessionId, seriesId, tenantId, managerId, reportId,
           });
           console.log(`[AI Pipeline] Summary emails sent for session ${sessionId}`);
         } catch (emailError) {
           console.error(`[AI Pipeline] Summary email failed for session ${sessionId}:`, emailError);
         }
         ```
       - Also add summary email in the catch block (when AI fails), so a degraded email still goes out:
         ```typescript
         // In the main catch block, after setting aiStatus to "failed":
         try {
           await sendPostSessionSummaryEmails({
             sessionId, seriesId, tenantId, managerId, reportId,
           });
         } catch (emailError) {
           console.error("[AI Pipeline] Summary email failed after AI failure:", emailError);
         }
         ```

    3. **Org language setting** -- add preferredLanguage to company settings:
       - In `src/app/(dashboard)/settings/company/page.tsx`: Add a language selector (Select component from shadcn/ui) in the company settings form. Options: English (en), Romanian (ro), German (de), French (fr), Spanish (es), Portuguese (pt). Store as `preferredLanguage` in the tenant's settings JSONB. Default "en".
       - In the company settings API route (check if PATCH exists at `src/app/api/settings/company/route.ts` or similar): Add preferredLanguage to the accepted fields. Validate with Zod: `z.enum(["en", "ro", "de", "fr", "es", "pt"]).optional()`.
       - Label the setting as "Organization Language" with help text "AI-generated content (summaries, nudges, suggestions) will use this language."

    4. **AI prompt language injection** (`src/lib/ai/service.ts`):
       - In each AI generation function (generateSummary, generateManagerAddendum, generateActionSuggestions, generateNudges): accept an optional `language?: string` parameter.
       - When language is set and not "en", append to the system prompt: `\n\nIMPORTANT: Respond entirely in ${languageName}. All text content must be in ${languageName}.` where languageName maps the code to full name (ro -> Romanian, de -> German, etc.).
       - In `gatherSessionContext` or `pipeline.ts`: fetch the tenant's preferredLanguage from the tenants table and pass it through to each AI function.
       - Per locked decision: this affects ALL AI output (summaries, nudges, addendum, suggestions) -- not just emails.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - sendPostSessionSummaryEmails fetches session data, renders manager/report variants, and sends via SMTP
    - AI pipeline calls sendPostSessionSummaryEmails after completion (and after failure for degraded email)
    - Company settings page has a language selector stored in tenants.settings.preferredLanguage
    - AI service functions accept language parameter and include it in system prompts
    - Pipeline fetches tenant language and passes to AI functions
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire notification scheduling into session completion and series management</name>
  <files>
    src/app/api/sessions/[id]/complete/route.ts
    src/app/api/series/route.ts
    src/app/api/series/[id]/route.ts
    src/lib/db/seed.ts
  </files>
  <action>
    1. **Wire scheduling into session completion** (`src/app/api/sessions/[id]/complete/route.ts`):
       - Import `scheduleSeriesNotifications` from `@/lib/notifications/scheduler`
       - After `runAIPipelineDirect` is called (fire-and-forget), add notification scheduling for the NEXT session:
         ```typescript
         // Schedule notifications for the next session (non-blocking)
         scheduleSeriesNotifications({
           tenantId: session.user.tenantId,
           seriesId: result.seriesId,
           managerId: result.managerId,
           reportId: result.reportId,
           nextSessionAt: new Date(result.nextSessionAt),
           reminderHoursBefore: series.reminderHoursBefore ?? 24,
           managerName: /* fetch from series query or add to result */,
           reportName: /* fetch from series query or add to result */,
         }).catch(err => console.error("Failed to schedule notifications:", err));
         ```
       - The completion endpoint already fetches the series record. Add manager and report user names to the series query (join users table for managerId and reportId to get firstName + lastName). Pass these through the result object so they're available for scheduling.

    2. **Wire scheduling into series creation** (`src/app/api/series/route.ts`):
       - After a new series is created and nextSessionAt is computed, schedule notifications:
         ```typescript
         if (newSeries.nextSessionAt) {
           await scheduleSeriesNotifications({
             tenantId, seriesId: newSeries.id,
             managerId, reportId,
             nextSessionAt: newSeries.nextSessionAt,
             reminderHoursBefore: 24, // default
             managerName, reportName,
           });
         }
         ```

    3. **Wire cancellation into series updates** (`src/app/api/series/[id]/route.ts`):
       - If series status changes to 'paused' or 'archived': cancel all pending notifications for that series via `cancelSeriesNotifications(seriesId, ['pre_meeting', 'agenda_prep'])`
       - If nextSessionAt changes: cancel old + schedule new notifications
       - If reminderHoursBefore changes: cancel old + schedule new notifications with updated timing

    4. **Update seed data** (`src/lib/db/seed.ts`):
       - Add `preferredLanguage: "en"` to the seed tenant's settings JSONB (so it exists for testing)
       - Add a couple sample notification records (pending pre_meeting and agenda_prep) for the seed series, with scheduledFor in the future, to verify the cron endpoint can pick them up during manual testing

    5. **Verify integration**: Ensure the completion flow is:
       Session complete -> compute nextSessionAt -> update series -> fire AI pipeline (async) -> schedule next session notifications (async)
       AI pipeline complete -> send summary emails (within pipeline) -> done
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -30 && echo "---" && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Session completion schedules pre_meeting and agenda_prep notifications for the next session
    - Series creation schedules notifications when nextSessionAt is set
    - Series pause/archive cancels pending notifications
    - Series reminder setting changes reschedule notifications
    - Seed data includes sample notification records and tenant preferredLanguage
    - Full pipeline works: complete session -> AI runs -> summary emails fire -> next session notifications scheduled
    - TypeScript compiles and build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `bun run build` succeeds
- AI pipeline.ts imports and calls sendPostSessionSummaryEmails
- Complete endpoint imports and calls scheduleSeriesNotifications
- Series API routes handle notification scheduling/cancellation
- Company settings page shows language selector
- AI service functions accept and use language parameter
</verification>

<success_criteria>
- Post-session summary email is sent to both manager and report after AI pipeline completes
- Manager gets extra AI addendum section, report gets shared content only
- If AI fails, degraded summary email still fires with session data
- Completing a session schedules pre-meeting and agenda prep notifications for the next session
- Series lifecycle changes (pause, archive, reschedule) correctly cancel/reschedule notifications
- Org admin can set preferred language, and AI content generates in that language
- No manual steps required -- all notification triggers are automatic
</success_criteria>

<output>
After completion, create `.planning/phases/09-email-notifications/09-02-SUMMARY.md`
</output>
