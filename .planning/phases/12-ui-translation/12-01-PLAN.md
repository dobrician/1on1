---
phase: 12-ui-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/i18n/zod-error-map.ts
  - messages/en/validation.json
  - messages/ro/validation.json
  - messages/en/common.json
  - messages/ro/common.json
  - src/i18n/request.ts
  - src/app/(auth)/reset-password/page.tsx
  - src/app/(auth)/verify-email/page.tsx
  - src/app/(auth)/invite/[token]/page.tsx
  - src/components/auth/email-verification-banner.tsx
  - messages/en/auth.json
  - messages/ro/auth.json
autonomous: true
requirements: [VALD-01, VALD-02, UITR-03]

must_haves:
  truths:
    - "A Romanian-locale user submitting an invalid form sees validation errors in Romanian"
    - "API error responses (rate limits, permission denied) display in the user's UI language in toasts"
    - "All auth pages display translated strings with no hardcoded English visible"
  artifacts:
    - path: "src/lib/i18n/zod-error-map.ts"
      provides: "useZodI18nErrors hook for translated Zod validation errors"
      exports: ["useZodI18nErrors"]
    - path: "messages/en/validation.json"
      provides: "English validation error messages"
      contains: "validation"
    - path: "messages/ro/validation.json"
      provides: "Romanian validation error messages"
      contains: "validation"
  key_links:
    - from: "src/lib/i18n/zod-error-map.ts"
      to: "messages/*/validation.json"
      via: "useTranslations('validation')"
      pattern: "useTranslations.*validation"
    - from: "messages/en/common.json"
      to: "client error handlers"
      via: "t('common.errors.*')"
      pattern: "errors\\."
---

<objective>
Create translated validation error infrastructure and complete all auth page translations.

Purpose: Establish the Zod error translation hook and API error key map that all forms and API calls will use, then finish the remaining untranslated auth pages (reset-password, verify-email, invite, email-verification-banner).
Output: useZodI18nErrors hook, validation namespace (EN+RO), error keys in common namespace, all auth pages fully translated.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-translation/12-RESEARCH.md
@.planning/phases/11-i18n-foundation/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation namespace and Zod error translation hook</name>
  <files>src/lib/i18n/zod-error-map.ts, messages/en/validation.json, messages/ro/validation.json, messages/en/common.json, messages/ro/common.json, src/i18n/request.ts</files>
  <action>
1. Create `messages/en/validation.json` with keys under `"validation"` namespace:
   - `required`: "This field is required"
   - `invalidEmail`: "Please enter a valid email address"
   - `minLength`: "Must be at least {min} characters" (ICU param)
   - `maxLength`: "Must be at most {max} characters" (ICU param)
   - `passwordUppercase`: "Must contain at least one uppercase letter"
   - `passwordLowercase`: "Must contain at least one lowercase letter"
   - `passwordNumber`: "Must contain at least one number"
   - `passwordsMatch`: "Passwords don't match"
   - `invalidUrl`: "Must be a valid URL"
   - `invalidType`: "Invalid value"

2. Create `messages/ro/validation.json` with Romanian translations for all keys above.

3. Add error keys to `messages/en/common.json` under `"common.errors"`:
   - `unauthorized`: "Please sign in to continue"
   - `forbidden`: "You don't have permission to do this"
   - `notFound`: "The requested resource was not found"
   - `badRequest`: "Invalid request"
   - `rateLimited`: "Too many requests. Please try again later"
   - `serverError`: "Something went wrong. Please try again."
   - `networkError`: "Unable to connect. Check your internet connection."

4. Add matching Romanian translations to `messages/ro/common.json`.

5. Update `src/i18n/request.ts` to import and spread the new `validation.json` namespace alongside existing namespaces.

6. Create `src/lib/i18n/zod-error-map.ts` exporting `useZodI18nErrors()` hook:
   - Import `useTranslations` from 'next-intl' and `z, ZodIssueCode` from 'zod'
   - Call `useTranslations('validation')` to get `t`
   - Call `z.setErrorMap()` with a function that maps:
     - `too_small` with string type + minimum=1 -> `t('required')`
     - `too_small` with string type -> `t('minLength', { min: issue.minimum })`
     - `too_big` with string type -> `t('maxLength', { max: issue.maximum })`
     - `invalid_string` with email validation -> `t('invalidEmail')`
     - `invalid_type` with received='undefined' -> `t('required')`
     - Default -> `ctx.defaultError`
   - The hook returns void, just sets the global error map with current locale translations

IMPORTANT: The hook uses `useTranslations` which requires React context -- it must be called inside a component, not in a utility function. Keep schemas in `src/lib/validations/` language-agnostic (no t() calls in schemas).
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>validation.json namespace exists for EN and RO, common.json has error keys for both locales, useZodI18nErrors hook compiles and exports correctly, request.ts loads validation namespace</done>
</task>

<task type="auto">
  <name>Task 2: Translate remaining auth pages</name>
  <files>src/app/(auth)/reset-password/page.tsx, src/app/(auth)/verify-email/page.tsx, src/app/(auth)/invite/[token]/page.tsx, src/components/auth/email-verification-banner.tsx, messages/en/auth.json, messages/ro/auth.json</files>
  <action>
For each untranslated auth component, follow the established pattern from login page (Phase 11):

1. **reset-password/page.tsx**: Import `useTranslations` from 'next-intl'. Replace all hardcoded strings with `t('auth.resetPassword.*')` calls. Add any missing keys to `messages/en/auth.json` and `messages/ro/auth.json` under an `auth.resetPassword` sub-object (title, description, newPassword placeholder, confirmPassword placeholder, submit button, success message, error messages). Also call `useZodI18nErrors()` from `src/lib/i18n/zod-error-map.ts` at top of component so Zod validation errors display translated.

2. **verify-email/page.tsx**: Import `useTranslations`. Replace hardcoded strings with `t('auth.verifyEmail.*')` calls. Add keys for title, description, checking message, success, error, resend link text.

3. **invite/[token]/page.tsx** (and any sub-components in that directory): Import `useTranslations`. Replace hardcoded strings with `t('auth.invite.*')` calls. Add keys for accept invite title, setting password, welcome message, error states (expired, invalid token). Call `useZodI18nErrors()` if component has a form.

4. **email-verification-banner.tsx**: Import `useTranslations`. Replace hardcoded strings with `t('auth.verificationBanner.*')` calls. Add keys for banner message and resend button text.

For each file: check if it's a Server Component (no 'use client') -- if so, use `getTranslations` from 'next-intl/server' instead of `useTranslations`. The `useZodI18nErrors` hook can only be used in Client Components.

Add all new translation keys to BOTH `messages/en/auth.json` AND `messages/ro/auth.json`.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -30 && bun run build 2>&1 | tail -20</automated>
  </verify>
  <done>All auth pages (reset-password, verify-email, invite, email-verification-banner) use t() calls with zero hardcoded English strings. Zod validation errors in auth forms display via useZodI18nErrors. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `bun run build` succeeds
- `grep -r "toLocaleDateString" src/app/\(auth\)/` returns no matches
- All auth page files import from 'next-intl'
</verification>

<success_criteria>
- useZodI18nErrors hook exists and compiles
- validation.json namespace has EN + RO translations
- common.json has error keys for EN + RO
- All 4 remaining auth pages/components use t() calls exclusively
- No hardcoded English strings in any auth page
- TypeScript and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-translation/12-01-SUMMARY.md`
</output>
