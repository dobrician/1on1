---
phase: 12-ui-translation
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/components/session/question-widget.tsx
  - src/components/session/widgets/rating-1-10-widget.tsx
  - src/components/session/widgets/multiple-choice-widget.tsx
  - src/components/session/widgets/text-widget.tsx
  - src/components/session/notes-editor.tsx
  - src/components/session/save-status.tsx
  - src/components/session/wizard-navigation.tsx
  - src/components/session/wizard-step-sidebar.tsx
  - src/components/session/wizard-mobile-carousel.tsx
  - src/components/session/recap-screen.tsx
  - src/components/session/talking-point-list.tsx
  - src/components/session/action-item-inline.tsx
  - src/components/session/nudge-list.tsx
  - src/components/session/ai-suggestions-section.tsx
  - src/components/session/ai-summary-section.tsx
  - src/components/session/wizard-top-bar.tsx
  - src/components/session/context-panel.tsx
  - src/components/session/floating-context-widgets.tsx
  - src/components/session/summary-screen.tsx
  - src/components/session/session-summary-view.tsx
  - src/components/session/question-history-dialog.tsx
  - messages/en/sessions.json
  - messages/ro/sessions.json
autonomous: true
requirements: [UITR-05]

must_haves:
  truths:
    - "All session wizard components display translated strings with no hardcoded English"
    - "Session dates in wizard, summary, and context panel use locale-aware formatting"
    - "Widget labels (rating, text, multiple choice) are translated"
  artifacts:
    - path: "src/components/session/question-widget.tsx"
      provides: "Translated question widget dispatcher"
    - path: "src/components/session/recap-screen.tsx"
      provides: "Translated recap with locale-aware dates"
    - path: "src/components/session/wizard-navigation.tsx"
      provides: "Translated wizard navigation buttons"
  key_links:
    - from: "src/components/session/question-widget.tsx"
      to: "messages/*/sessions.json"
      via: "useTranslations('sessions')"
      pattern: "useTranslations.*sessions"
    - from: "src/components/session/recap-screen.tsx"
      to: "next-intl useFormatter"
      via: "format.dateTime() replacing toLocaleDateString"
      pattern: "useFormatter|format\\.dateTime"
---

<objective>
Translate all session wizard components -- the largest area of untranslated UI.

Purpose: The session wizard is the core user interaction. All ~20 session components need translation wiring and date formatting fixes so the entire meeting experience works in any supported language.
Output: Every session component uses t() calls and locale-aware formatters with zero hardcoded English.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-translation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate wizard widgets and core components</name>
  <files>src/components/session/question-widget.tsx, src/components/session/widgets/rating-1-10-widget.tsx, src/components/session/widgets/multiple-choice-widget.tsx, src/components/session/widgets/text-widget.tsx, src/components/session/notes-editor.tsx, src/components/session/save-status.tsx, src/components/session/wizard-navigation.tsx, src/components/session/wizard-step-sidebar.tsx, src/components/session/wizard-mobile-carousel.tsx, messages/en/sessions.json, messages/ro/sessions.json</files>
  <action>
For each TODO component (no existing translation imports):

1. **Add `import { useTranslations } from 'next-intl'`** and `const t = useTranslations('sessions');` at the top of the component.

2. **Replace every hardcoded English string** with a `t()` call using the existing key from `messages/en/sessions.json`. If a key doesn't exist, add it to both EN and RO JSON files.

Specific components:

- **question-widget.tsx**: Translate any labels, placeholders, or helper text. This is the dispatcher that renders sub-widgets -- check for hardcoded "Required", "Optional", etc.

- **rating-1-10-widget.tsx**: Translate scale labels (e.g., "Low"/"High" endpoint labels), any helper text. Follow the pattern from rating-1-5-widget.tsx which already uses translations.

- **multiple-choice-widget.tsx**: Translate "Select one", "Select all that apply", any placeholder text. Follow yes-no-widget.tsx pattern.

- **text-widget.tsx**: Translate placeholder text ("Type your answer..."), character count labels. Follow existing widget patterns.

- **notes-editor.tsx**: Translate placeholder ("Add private notes..."), any toolbar labels, save indicator text.

- **save-status.tsx**: Translate "Saving...", "All changes saved", "Error saving" status messages.

- **wizard-navigation.tsx**: Translate "Previous", "Next", "Complete Session" button labels.

- **wizard-step-sidebar.tsx**: Translate any "Steps", "Progress" labels. Step names come from data, not hardcoded.

- **wizard-mobile-carousel.tsx**: Translate accessibility labels, any hardcoded text in the mobile carousel UI.

Add any new keys to `messages/en/sessions.json` under appropriate sub-objects (e.g., `sessions.widgets.rating10.low`, `sessions.wizard.next`). Add corresponding Romanian translations to `messages/ro/sessions.json`.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 9 widget and core wizard components import useTranslations and use t() for every visible string. No hardcoded English remains.</done>
</task>

<task type="auto">
  <name>Task 2: Translate context panel, summaries, and fix all session date formatting</name>
  <files>src/components/session/recap-screen.tsx, src/components/session/talking-point-list.tsx, src/components/session/action-item-inline.tsx, src/components/session/nudge-list.tsx, src/components/session/ai-suggestions-section.tsx, src/components/session/ai-summary-section.tsx, src/components/session/wizard-top-bar.tsx, src/components/session/context-panel.tsx, src/components/session/floating-context-widgets.tsx, src/components/session/summary-screen.tsx, src/components/session/session-summary-view.tsx, src/components/session/question-history-dialog.tsx, messages/en/sessions.json, messages/ro/sessions.json</files>
  <action>
Two sub-tasks in one: (A) translate remaining TODO components, (B) fix date formatting in all session components that have `toLocaleDateString("en-US")`.

**A. Translate TODO components:**

For each component without translations, add `useTranslations('sessions')` and replace all hardcoded strings:

- **recap-screen.tsx**: Translate "Recap", "Previous session summary", action item labels, date displays. Has 4 `toLocaleDateString` calls to fix.
- **talking-point-list.tsx**: Translate "Talking Points", "Add talking point", "No talking points", "Discussed" toggle labels.
- **action-item-inline.tsx**: Translate "Due:", "Completed", "Overdue" labels. Has `toLocaleDateString("en-US")`.
- **nudge-list.tsx**: Translate "AI Nudges", "No nudges available" empty state.
- **ai-suggestions-section.tsx**: Translate "AI Suggestions", "Loading suggestions...", empty state text.
- **ai-summary-section.tsx**: Translate "AI Summary", "Generating summary...", empty state text.

**B. Fix date formatting in ALL session components:**

For every file with `toLocaleDateString("en-US")` or hardcoded date formatting:

1. Import `useFormatter` from 'next-intl' (if not already). Add `const format = useFormatter();`.
2. Replace `toLocaleDateString("en-US", opts)` with `format.dateTime(new Date(date), opts)`.

Known files with date formatting to fix:
- `wizard-top-bar.tsx`: 1 occurrence
- `context-panel.tsx`: 2 occurrences (may already have `useFormatter` partially)
- `floating-context-widgets.tsx`: 2 occurrences (may already have `useFormatter` partially)
- `summary-screen.tsx`: 2 occurrences
- `session-summary-view.tsx`: 4 occurrences
- `question-history-dialog.tsx`: 1 occurrence (may already have `useFormatter`)
- `recap-screen.tsx`: 4 occurrences
- `action-item-inline.tsx`: 1 occurrence

For components that already partially use `useFormatter` (context-panel, floating-context-widgets, question-history-dialog), ensure ALL date calls use it -- remove any remaining `toLocaleDateString("en-US")` calls.

Add any new translation keys to both EN and RO session JSON files.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -20 && grep -rn 'toLocaleDateString\|"en-US"' src/components/session/ || echo "No hardcoded locale formatting found"</automated>
  </verify>
  <done>All 12 session support components translated. Zero toLocaleDateString("en-US") calls remain in any session component. All dates use format.dateTime(), all strings use t().</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -rn 'toLocaleDateString("en-US")' src/components/session/` returns no matches
- `bun run build` succeeds
- Every `.tsx` file in `src/components/session/` imports from 'next-intl' (except score-sparkline.tsx which is visual-only)
</verification>

<success_criteria>
- All ~20 session wizard components use useTranslations('sessions') for visible text
- All session date displays use useFormatter().dateTime()
- No hardcoded "en-US" locale strings in any session component
- New translation keys added to both EN and RO JSON files
- TypeScript passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-translation/12-03-SUMMARY.md`
</output>
