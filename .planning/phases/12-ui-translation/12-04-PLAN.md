---
phase: 12-ui-translation
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/components/people/manager-select.tsx
  - src/components/people/member-picker.tsx
  - src/components/people/team-card.tsx
  - src/components/people/team-create-dialog.tsx
  - src/components/people/invite-dialog.tsx
  - src/components/people/role-select.tsx
  - src/app/(dashboard)/people/[id]/profile-edit-form.tsx
  - src/app/(dashboard)/people/[id]/page.tsx
  - src/app/(dashboard)/teams/[id]/team-detail-client.tsx
  - src/components/templates/question-card.tsx
  - src/components/templates/answer-config-form.tsx
  - src/components/templates/conditional-logic-form.tsx
  - src/components/templates/template-editor.tsx
  - messages/en/people.json
  - messages/ro/people.json
  - messages/en/teams.json
  - messages/ro/teams.json
  - messages/en/templates.json
  - messages/ro/templates.json
autonomous: true
requirements: [UITR-06, UITR-07]

must_haves:
  truths:
    - "All people management pages display translated strings with no hardcoded English"
    - "All template builder components display translated strings with no hardcoded English"
    - "Toast messages from people and template operations display in user's locale"
    - "People profile page dates use locale-aware formatting"
  artifacts:
    - path: "src/components/people/manager-select.tsx"
      provides: "Translated manager selection component"
    - path: "src/components/templates/question-card.tsx"
      provides: "Translated question card in template editor"
  key_links:
    - from: "src/components/people/team-create-dialog.tsx"
      to: "messages/*/teams.json"
      via: "useTranslations('teams')"
      pattern: "useTranslations.*teams"
    - from: "src/components/templates/question-card.tsx"
      to: "messages/*/templates.json"
      via: "useTranslations('templates')"
      pattern: "useTranslations.*templates"
---

<objective>
Translate all people/teams management and template builder components.

Purpose: People management (invites, roles, profiles, teams) and template editing are admin-heavy areas that still have hardcoded strings and toast messages. Translate all remaining components so admins and managers get a fully localized experience.
Output: All people, team, and template components use t() calls with zero hardcoded English.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-translation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate people and teams components</name>
  <files>src/components/people/manager-select.tsx, src/components/people/member-picker.tsx, src/components/people/team-card.tsx, src/components/people/team-create-dialog.tsx, src/components/people/invite-dialog.tsx, src/components/people/role-select.tsx, src/app/(dashboard)/people/[id]/profile-edit-form.tsx, src/app/(dashboard)/people/[id]/page.tsx, src/app/(dashboard)/teams/[id]/team-detail-client.tsx, messages/en/people.json, messages/ro/people.json, messages/en/teams.json, messages/ro/teams.json</files>
  <action>
For each component, either add `useTranslations` (TODO components) or fix remaining hardcoded strings (DONE components with hardcoded toasts):

**TODO components (need full translation wiring):**

- **manager-select.tsx**: Import `useTranslations('people')`. Replace hardcoded toast messages ("Manager updated", error messages) with `t('people.managerSelect.*')` calls. Replace any hardcoded labels.

- **member-picker.tsx**: Import `useTranslations('people')`. Replace hardcoded toast messages and labels with `t()` calls.

- **team-card.tsx**: Import `useTranslations('teams')`. Replace hardcoded text ("members", team stats) with `t()` calls.

- **team-create-dialog.tsx**: Import `useTranslations('teams')`. Replace all dialog labels, placeholders, button text, and toast messages with `t()` calls. Call `useZodI18nErrors()` from `src/lib/i18n/zod-error-map.ts` if form uses zodResolver.

- **profile-edit-form.tsx**: Import `useTranslations('people')`. Replace all form labels, placeholders, and buttons with `t()` calls. Call `useZodI18nErrors()` if form uses zodResolver.

- **people/[id]/page.tsx**: Import `getTranslations` (Server Component) or `useTranslations` (Client Component). Replace hardcoded strings. Has `toLocaleDateString("en-US")` -- replace with `format.dateTime()` via `useFormatter`.

**DONE components with remaining hardcoded issues:**

- **invite-dialog.tsx**: Already uses `useTranslations`. Check for hardcoded toast error messages and replace with `t()` calls.

- **role-select.tsx**: Already uses `useTranslations`. Check for hardcoded toast messages and replace with `t()` calls.

- **team-detail-client.tsx**: Already uses `useTranslations`. Has `toLocaleDateString("en-US")` -- import `useFormatter` and replace with `format.dateTime()`. Check for any remaining hardcoded strings.

Add all new translation keys to both EN and RO JSON files (people.json and teams.json).
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -20 && grep -rn 'toLocaleDateString\|"en-US"' src/components/people/ src/app/\(dashboard\)/people/ src/app/\(dashboard\)/teams/ || echo "No hardcoded locale formatting found"</automated>
  </verify>
  <done>All people and teams components use t() for visible strings and toast messages. Zero toLocaleDateString("en-US") calls remain. Profile page dates use locale-aware formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Translate template builder components</name>
  <files>src/components/templates/question-card.tsx, src/components/templates/answer-config-form.tsx, src/components/templates/conditional-logic-form.tsx, src/components/templates/template-editor.tsx, messages/en/templates.json, messages/ro/templates.json</files>
  <action>
For each template component:

**TODO components (need full translation wiring):**

- **question-card.tsx**: Import `useTranslations('templates')`. Replace all hardcoded text: question type labels, edit/delete button labels, drag handle accessibility text, any "Required" badge text. Use keys from existing `messages/en/templates.json`.

- **answer-config-form.tsx**: Import `useTranslations('templates')`. Replace all form labels: "Answer Type", "Options", "Add Option", min/max labels, placeholder text for configuration fields. Add keys to templates.json if not present.

- **conditional-logic-form.tsx**: Import `useTranslations('templates')`. Replace all labels: "Condition", "Show if", "equals", operator labels, "Add condition", "Remove" buttons. Add keys to templates.json if not present.

**DONE component with remaining issues:**

- **template-editor.tsx**: Already uses `useTranslations`. Check for any hardcoded toast error messages (e.g., "Template saved", "Error saving template") and replace with `t()` calls.

Add all new translation keys to both `messages/en/templates.json` and `messages/ro/templates.json`.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -20 && grep -rn 'toLocaleDateString\|"en-US"' src/components/templates/ || echo "No hardcoded locale formatting found"</automated>
  </verify>
  <done>All 4 template components use t() for every visible string. No hardcoded English remains in template builder UI.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `bun run build` succeeds
- `grep -rn 'toLocaleDateString("en-US")' src/components/people/ src/components/templates/ src/app/(dashboard)/people/ src/app/(dashboard)/teams/` returns no matches
</verification>

<success_criteria>
- All people/teams management components use t() for all visible text and toast messages
- All template builder components use t() for all visible text
- Date displays in people pages use locale-aware formatting
- Forms with zodResolver call useZodI18nErrors()
- New keys added to both EN and RO JSON files
- TypeScript passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-translation/12-04-SUMMARY.md`
</output>
