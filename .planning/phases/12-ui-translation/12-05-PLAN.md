---
phase: 12-ui-translation
plan: 05
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/components/analytics/score-trend-chart.tsx
  - src/components/analytics/velocity-chart.tsx
  - src/components/analytics/adherence-chart.tsx
  - src/components/analytics/session-comparison.tsx
  - src/components/analytics/team-heatmap.tsx
  - src/app/(dashboard)/settings/audit-log/audit-log-client.tsx
  - src/app/(dashboard)/settings/audit-log/page.tsx
  - src/app/(dashboard)/analytics/page.tsx
  - messages/en/analytics.json
  - messages/ro/analytics.json
  - messages/en/settings.json
  - messages/ro/settings.json
autonomous: true
requirements: [UITR-08, UITR-09, UITR-10, UITR-01, UITR-02, FMT-04]

must_haves:
  truths:
    - "Analytics chart axes and tooltips display dates and numbers in locale-aware format"
    - "A Romanian user sees chart X-axis dates as DD.MM format, not MM/DD"
    - "Settings pages display translated strings with no hardcoded English"
    - "Command palette search UI displays in user's locale"
    - "Language picker in user menu works correctly (already built, verified here)"
    - "Navigation and layout strings are translated (already done, verified here)"
  artifacts:
    - path: "src/components/analytics/score-trend-chart.tsx"
      provides: "Locale-aware chart with translated labels and formatted axes"
    - path: "src/components/analytics/session-comparison.tsx"
      provides: "Locale-aware comparison chart"
    - path: "src/app/(dashboard)/settings/audit-log/page.tsx"
      provides: "Translated audit log page"
  key_links:
    - from: "src/components/analytics/score-trend-chart.tsx"
      to: "next-intl useFormatter"
      via: "format.dateTime() in Recharts tickFormatter closure"
      pattern: "tickFormatter.*format\\.dateTime"
    - from: "src/components/analytics/velocity-chart.tsx"
      to: "next-intl useFormatter"
      via: "format.dateTime() in Recharts tickFormatter closure"
      pattern: "tickFormatter.*format\\.dateTime"
---

<objective>
Translate analytics charts with locale-aware formatting, settings pages, and verify already-complete areas (nav, command palette, language switcher).

Purpose: Analytics charts are the most formatting-intensive area -- Recharts X-axes, tooltips, and data labels all need locale-aware date/number formatting. Settings and audit log pages need translation wiring. This plan also covers verification of UITR-01 (language switcher), UITR-02 (navigation), and UITR-10 (command palette) which are already built.
Output: All analytics charts use locale-aware formatters, settings fully translated, all Phase 12 requirements verified.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ui-translation/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locale-aware formatting in analytics charts</name>
  <files>src/components/analytics/score-trend-chart.tsx, src/components/analytics/velocity-chart.tsx, src/components/analytics/adherence-chart.tsx, src/components/analytics/session-comparison.tsx, src/components/analytics/team-heatmap.tsx, src/app/(dashboard)/analytics/page.tsx, messages/en/analytics.json, messages/ro/analytics.json</files>
  <action>
For each analytics chart component, apply Pattern 4 from the research (Recharts Locale-Aware Formatting):

**CRITICAL: Call `useFormatter()` at the component level (not inside Recharts callbacks). Capture the formatter in a variable, then use it inside tickFormatter/tooltip closures.**

1. **score-trend-chart.tsx** (3 occurrences of hardcoded formatting):
   - Import `useFormatter, useTranslations` from 'next-intl'
   - Add `const format = useFormatter(); const t = useTranslations('analytics');` at component top
   - Replace XAxis `tickFormatter` from `toLocaleDateString("en-US", ...)` to `(val) => format.dateTime(new Date(val), { month: 'short', day: 'numeric' })`
   - Replace custom Tooltip date formatting with `format.dateTime()`
   - Replace score number formatting with `format.number(score, { maximumFractionDigits: 2 })`
   - Replace any hardcoded "Score:" label with `t('chart.score')`

2. **velocity-chart.tsx** (1 occurrence):
   - Same pattern: import formatters, replace XAxis tickFormatter and tooltip date formatting
   - Replace hardcoded labels with `t('chart.*')` calls

3. **adherence-chart.tsx** (1 occurrence):
   - Same pattern: replace date formatting in axes and tooltips
   - Replace hardcoded labels

4. **session-comparison.tsx** (5 occurrences -- most formatting-heavy):
   - Import `useFormatter, useTranslations`
   - Replace all date formatting in XAxis, tooltips, and data labels
   - Replace hardcoded label strings with `t()` calls
   - Replace number formatting (`.toFixed()`) with `format.number()`

5. **team-heatmap.tsx** (1 occurrence):
   - Import `useFormatter` if needed
   - Replace any hardcoded date/number formatting
   - Add `useTranslations` for any labels

6. **analytics/page.tsx** (1 occurrence):
   - Check if Server Component -- use `getFormatter` from 'next-intl/server' if so
   - Replace any `toLocaleDateString` or hardcoded formatting
   - Already uses `getTranslations` -- verify all text uses `t()`

Add any missing translation keys to both `messages/en/analytics.json` and `messages/ro/analytics.json`.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -20 && grep -rn 'toLocaleDateString\|"en-US"\|\.toFixed' src/components/analytics/ src/app/\(dashboard\)/analytics/ || echo "No hardcoded locale formatting found"</automated>
  </verify>
  <done>All 5 chart components use useFormatter() for dates and numbers in tickFormatter closures and tooltips. Zero hardcoded "en-US" or .toFixed() calls remain in analytics. Chart labels use t() calls.</done>
</task>

<task type="auto">
  <name>Task 2: Translate settings pages and verify already-complete areas</name>
  <files>src/app/(dashboard)/settings/audit-log/audit-log-client.tsx, src/app/(dashboard)/settings/audit-log/page.tsx, messages/en/settings.json, messages/ro/settings.json</files>
  <action>
**A. Translate settings pages:**

1. **audit-log/audit-log-client.tsx**: Already uses `useTranslations`. Has `toLocaleDateString("en-US")` -- import `useFormatter` and replace with `format.dateTime()`. Check for any remaining hardcoded strings.

2. **audit-log/page.tsx**: Import `getTranslations` from 'next-intl/server' (Server Component). Replace any hardcoded page title or text with `t()` calls. Add keys to settings.json if not present.

Add any new keys to both EN and RO settings.json.

**B. Verify already-complete areas (no code changes expected, just confirm):**

Run these checks and note results in the summary:

3. **UITR-01 (Language switcher)**: Verify `src/components/layout/user-menu.tsx` has working language switcher with `useTranslations`. Already confirmed in Phase 11 -- just note as verified.

4. **UITR-02 (Navigation/layout)**: Verify `sidebar.tsx`, `top-nav.tsx`, `user-menu.tsx` all use `useTranslations('navigation')`. Research shows all DONE -- just note as verified.

5. **UITR-10 (Command palette)**: Verify `src/components/search/command-palette.tsx` uses `useTranslations('search')`. Research shows DONE -- just note as verified.

If any of these verification checks reveal issues (unexpected hardcoded strings), fix them. Otherwise just confirm in the summary.
  </action>
  <verify>
    <automated>cd /home/dc/work/1on1 && npx tsc --noEmit 2>&1 | head -20 && grep -rn 'toLocaleDateString\|"en-US"' src/app/\(dashboard\)/settings/ || echo "No hardcoded locale formatting in settings" && bun run build 2>&1 | tail -10</automated>
  </verify>
  <done>Audit log pages use locale-aware date formatting and translated strings. Settings pages fully translated. Language switcher (UITR-01), navigation (UITR-02), and command palette (UITR-10) verified as complete. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `bun run build` succeeds
- `grep -rn 'toLocaleDateString("en-US")' src/components/analytics/ src/app/(dashboard)/settings/ src/app/(dashboard)/analytics/` returns no matches
- No `.toFixed()` calls remain in analytics display code
- Final build-wide check: `grep -rn 'toLocaleDateString("en-US")' src/` should return only `src/lib/notifications/sender.ts` (email -- Phase 13 scope)
</verification>

<success_criteria>
- All Recharts chart components use useFormatter() for date/number formatting in axes and tooltips
- Settings audit log uses locale-aware date formatting
- UITR-01 (language switcher), UITR-02 (navigation), UITR-10 (command palette) confirmed working
- No hardcoded "en-US" locale strings remain in any UI component (only email sender for Phase 13)
- TypeScript passes, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-translation/12-05-SUMMARY.md`
</output>
