---
phase: 27-ui-integration
plan: "04"
type: execute
wave: 4
depends_on:
  - "27-03"
files_modified: []
autonomous: false
requirements:
  - CORR-01
  - CORR-03
  - WFLOW-04
  - WFLOW-05
must_haves:
  truths:
    - "Full test suite passes with zero failures"
    - "TypeScript compiles with zero errors"
    - "ESLint passes with zero errors"
    - "Production build succeeds"
    - "Corrected answer rows show Amended badge in amber"
    - "Edit icon opens inline correction form (visible to managers only)"
    - "AI feedback appears in the reason field after 800ms"
    - "Correction history panel is at the bottom of the session page"
  artifacts:
    - path: "src/components/session/amended-badge.tsx"
      provides: "AmendedBadge component"
      exports: ["AmendedBadge"]
    - path: "src/components/session/correction-history-panel.tsx"
      provides: "CorrectionHistoryPanel component"
      exports: ["CorrectionHistoryPanel", "CorrectionEntry"]
    - path: "src/components/session/answer-correction-form.tsx"
      provides: "AnswerCorrectionForm component with AI feedback"
      exports: ["AnswerCorrectionForm"]
    - path: "src/app/(dashboard)/sessions/[id]/summary/page.tsx"
      provides: "Server page with history data join"
      contains: "sessionAnswerHistory"
  key_links:
    - from: "test suite"
      to: "all 4 phase 27 components"
      via: "bun run test passes"
      pattern: "PASS.*session/__tests__"
    - from: "production build"
      to: "all modified files"
      via: "bun run build succeeds"
      pattern: "Compiled successfully"
---

<objective>
Phase gate — run full automated quality suite, fix any remaining issues, then verify the correction UI visually on the live session detail page.

Purpose: Confirm Phase 27 is shippable — zero test failures, zero TypeScript errors, zero lint errors, production build succeeds, and the correction workflow is manually verified end-to-end.
Output: Phase 27 complete and verified. All requirements CORR-01, CORR-03, WFLOW-04, WFLOW-05 shipped.
</objective>

<execution_context>
@/home/dc/.claude/get-shit-done/workflows/execute-plan.md
@/home/dc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-ui-integration/27-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated quality gate</name>
  <files></files>
  <action>
Run the full quality suite in order. Fix any issues before proceeding to the checkpoint.

```bash
# 1. Full test suite
bun run test --run

# 2. TypeScript check
bun run typecheck

# 3. Lint
bun run lint

# 4. Production build
bun run build
```

**Common issues to fix:**

- TypeScript error on `SummaryAnswer` missing `id` — ensure the `id` field was added to the type in session-summary-view.tsx and the page's answerMap builds it correctly
- TypeScript error on `correctionsByAnswerId` — ensure the Record type uses `CorrectionEntry[]` from correction-history-panel.tsx
- Lint error `@typescript-eslint/no-explicit-any` — replace `unknown` for jsonb fields where needed
- Build error on missing module — if any component import path is wrong, correct it
- Test failure from mock mismatch in answer-correction-form.test.tsx — if the test asserts on text from `useTranslations` and the mock returns the key not the translation, align test assertions to match what the mock returns

Fix all issues discovered. Re-run each failing check after fix until all 4 commands succeed.
  </action>
  <verify>
    <automated>bun run test --run && bun run typecheck && bun run lint && bun run build</automated>
  </verify>
  <done>All 4 commands complete with zero errors. `bun run test --run` shows all tests passing. `bun run build` outputs "Compiled successfully".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of correction UI end-to-end</name>
  <what-built>
Complete Phase 27 correction UI:
- "Amended" amber badge on corrected answer rows (CORR-01)
- Inline correction form with before/after values when manager clicks edit icon (WFLOW-05)
- AI feedback (pass/fail + one sentence) below the reason textarea (WFLOW-04)
- Correction history panel at the bottom of the session page (CORR-03)
  </what-built>
  <how-to-verify>
**Prerequisites:** You need a completed session where at least one answer has been corrected via the API (from Phases 25–26). If no correction exists, you can create one via:

```bash
# Replace SESSION_ID, ANSWER_ID with real IDs from your dev DB
curl -X POST http://localhost:4300/api/sessions/SESSION_ID/corrections \
  -H "Content-Type: application/json" \
  -H "Cookie: authjs.session-token=YOUR_SESSION_COOKIE" \
  -d '{"answerId":"ANSWER_ID","reason":"Testing correction UI — original answer had a typo in the numeric value","newAnswerText":"Corrected text value"}'
```

**Verification steps:**

1. Navigate to a completed session detail page: `https://1on1.surmont.co/sessions/[sessionId]/summary`

2. **CORR-01 — Amended badge:**
   - Find an answer row that was corrected
   - Verify an amber "Amended" (EN) or "Modificat" (RO) badge appears inline next to the question text
   - Verify uncorrected answer rows have NO badge

3. **WFLOW-05 — Inline correction form:**
   - Log in as the manager of the session
   - Find any answer row — verify a small pencil icon (edit button) appears in the top-right of the answer card
   - Click the pencil icon
   - Verify a form opens INLINE (no page navigation) showing:
     - "Original" section with the current answer value (read-only)
     - "New answer" input field
     - "Correction reason" textarea
     - Submit and Cancel buttons
   - Click Cancel — verify the form closes
   - Log in as a member/report — verify NO pencil icons are visible

4. **WFLOW-04 — AI feedback on reason:**
   - Open a correction form (as manager)
   - Type fewer than 20 characters in the reason field — verify no AI feedback appears
   - Type 20+ characters and wait ~1 second
   - Verify either: loading spinner briefly, then green checkmark + feedback text (if AI approved), OR amber warning + feedback text (if AI flagged it)
   - Verify the Submit button remains enabled even when AI shows a warning

5. **CORR-03 — Correction history panel:**
   - Scroll to the bottom of the session page (before the back link)
   - On a session WITH corrections: verify the "Correction History" panel is expanded by default, shows actor name, timestamp, and reason
   - On a session WITHOUT corrections: verify the panel trigger is visible but collapsed

6. **i18n check:**
   - Switch language to Romanian
   - Verify "Amended" becomes "Modificat", "Correction History" becomes "Istoric corecții"
   - Verify no raw i18n keys (e.g., `corrections.amendedBadge`) are visible
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe any issues found</resume-signal>
  <files></files>
  <action>Human verification of complete correction UI workflow — see how-to-verify above for exact steps.</action>
  <verify><automated>MISSING — this is a human checkpoint task</automated></verify>
  <done>User types "approved" after verifying all 6 checks in how-to-verify above.</done>
</task>

</tasks>

<verification>
```bash
# Final confirmation all automated checks green
bun run test --run 2>&1 | tail -3
bun run typecheck 2>&1 | tail -3
bun run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- `bun run test --run` — all tests pass, zero failures
- `bun run typecheck` — zero TypeScript errors
- `bun run lint` — zero ESLint errors
- `bun run build` — production build succeeds
- Human verified: Amended badges visible on corrected rows (CORR-01)
- Human verified: Inline correction form opens without page navigation (WFLOW-05)
- Human verified: AI feedback appears after 800ms debounce (WFLOW-04)
- Human verified: Correction history panel at session bottom (CORR-03)
- Human verified: RO language shows translated strings, not raw keys
</success_criteria>

<output>
After completion, create `.planning/phases/27-ui-integration/27-04-SUMMARY.md`
</output>
